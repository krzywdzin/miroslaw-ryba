// Reagraph's GraphCanvas handles node tooltips natively via the label system.
// Node labels display automatically on hover when labelType="auto" is set.
// This file exists as a placeholder in case custom tooltip rendering is needed later.
//
// The GraphViewer component uses labelType="auto" which shows labels on hover,
// providing tooltip-like behavior without a separate component.

export {}
