---
phase: 03-configuration-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/label.tsx
  - src/components/ui/form.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/card.tsx
  - src/features/settings/schemas/config.schema.ts
  - src/features/settings/hooks/useConfigStore.ts
  - src/locales/pl/settings.json
  - src/locales/en/settings.json
  - src/i18n/config.ts
  - src/features/settings/SettingsPage.tsx
  - src/features/settings/components/SectionNav.tsx
  - src/features/settings/components/ApiKeysSection.tsx
  - src/features/settings/components/ModelSection.tsx
  - src/features/settings/components/SimulationSection.tsx
  - tests/features/settings/config-schema.test.ts
  - tests/features/settings/config-store.test.ts
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-06, CONF-07]

must_haves:
  truths:
    - "User can enter API key and base URL in text fields with on-blur validation"
    - "User can select model from presets (qwen-plus, GPT-4) or type custom model name"
    - "User can adjust agent count and round count via sliders, toggle Twitter/Reddit platforms"
    - "Configuration auto-saves to localStorage on valid field changes (debounced)"
    - "All field labels and section headings display in Polish (default) or English"
    - "User can test LLM API connection with inline Testuj button showing success/failure"
  artifacts:
    - path: "src/features/settings/hooks/useConfigStore.ts"
      provides: "Zustand store with persist middleware for all config values"
      exports: ["useConfigStore"]
    - path: "src/features/settings/schemas/config.schema.ts"
      provides: "Zod validation schemas for all config sections"
      exports: ["apiKeysSchema", "modelSchema", "simulationSchema", "zepSchema"]
    - path: "src/features/settings/SettingsPage.tsx"
      provides: "Main scrollable settings page with section navigation"
      min_lines: 40
    - path: "src/features/settings/components/ApiKeysSection.tsx"
      provides: "API key + base URL form with connection test button"
      min_lines: 50
    - path: "src/features/settings/components/ModelSection.tsx"
      provides: "Model preset select + custom input + boost model fields"
      min_lines: 40
    - path: "src/features/settings/components/SimulationSection.tsx"
      provides: "Agent count/rounds sliders + platform toggles"
      min_lines: 40
  key_links:
    - from: "src/features/settings/components/ApiKeysSection.tsx"
      to: "src/features/settings/hooks/useConfigStore.ts"
      via: "useConfigStore hook for reading/writing config"
      pattern: "useConfigStore"
    - from: "src/features/settings/SettingsPage.tsx"
      to: "src/features/settings/components/SectionNav.tsx"
      via: "SectionNav rendered alongside section components"
      pattern: "SectionNav"
    - from: "src/features/settings/hooks/useConfigStore.ts"
      to: "localStorage"
      via: "Zustand persist middleware with key mirofish-config"
      pattern: "persist.*mirofish-config"
---

<objective>
Install form dependencies, create shadcn UI components, build the Zustand config store with localStorage persistence, Zod validation schemas, i18n settings namespace, and implement the first 3 configuration sections (API Keys, Model Selection, Simulation Parameters) with auto-save, live validation, and LLM connection testing.

Purpose: Delivers the core configuration experience -- users can set API keys, choose models, and configure simulation parameters with immediate persistence and validation feedback. This is the foundation all other config sections build on.

Output: Working settings page with 3 of 5 sections, scrollable layout with section navigation, auto-save to localStorage, and LLM connection test.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-configuration-panel/03-CONTEXT.md
@.planning/phases/03-configuration-panel/03-RESEARCH.md
@.planning/phases/03-configuration-panel/03-UI-SPEC.md
@.planning/phases/03-configuration-panel/03-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/api/client.ts:
```typescript
import { z } from 'zod/v4'
export async function apiRequest<T>(path: string, options?: ApiRequestOptions): Promise<T>
```

From src/hooks/usePolling.ts:
```typescript
export function usePollingQuery<T>(
  queryKey: readonly unknown[],
  queryFn: () => Promise<T>,
  options: { isComplete: (data: T) => boolean; interval?: number; enabled?: boolean }
)
```

From src/lib/error-handler.ts:
```typescript
export async function handleApiError(error: unknown, options?: { retryFn?: () => Promise<void>; context?: string }): Promise<void>
```

From src/i18n/config.ts:
```typescript
// Resources registered via static imports, namespaces: common, navigation, dashboard, errors
// Must add 'settings' namespace with static imports
```

From src/app/App.tsx:
```typescript
// SettingsPage imported from '@/features/settings/SettingsPage' at route '/settings'
```

Existing shadcn components: button, skeleton, badge, separator, tooltip, breadcrumb, collapsible, sheet, dropdown-menu, alert, sonner
Zod import: always from 'zod/v4' (project convention)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create shadcn components, Zustand config store, Zod schemas, and i18n settings namespace</name>
  <files>
    package.json, pnpm-lock.yaml,
    src/components/ui/input.tsx, src/components/ui/select.tsx, src/components/ui/switch.tsx,
    src/components/ui/label.tsx, src/components/ui/form.tsx, src/components/ui/slider.tsx,
    src/components/ui/card.tsx,
    src/features/settings/schemas/config.schema.ts,
    src/features/settings/hooks/useConfigStore.ts,
    src/locales/pl/settings.json, src/locales/en/settings.json,
    src/i18n/config.ts,
    tests/features/settings/config-schema.test.ts,
    tests/features/settings/config-store.test.ts
  </files>
  <read_first>
    src/components/ui/button.tsx (shadcn component pattern reference -- data-slot, cn(), CVA),
    src/i18n/config.ts (i18n namespace registration pattern),
    src/lib/utils.ts (cn utility),
    src/styles/globals.css (Tailwind v4 theme variables),
    .planning/phases/03-configuration-panel/03-RESEARCH.md (Zustand store pattern, Zod schema examples),
    .planning/phases/03-configuration-panel/03-UI-SPEC.md (copywriting contract, all Polish/English strings)
  </read_first>
  <action>
    1. Install form dependencies: `pnpm add react-hook-form @hookform/resolvers`

    2. Create shadcn UI components manually (same pattern as Phase 2 -- CLI has permission issues):
       - `src/components/ui/input.tsx` -- standard shadcn Input with data-slot="input", cn(), forwardRef
       - `src/components/ui/select.tsx` -- shadcn Select (Radix UI Select primitives with data-slot attributes)
       - `src/components/ui/switch.tsx` -- shadcn Switch (Radix UI Switch)
       - `src/components/ui/label.tsx` -- shadcn Label (Radix UI Label, 13px semibold per UI-SPEC)
       - `src/components/ui/form.tsx` -- shadcn Form integration with React Hook Form (FormField, FormItem, FormLabel, FormControl, FormMessage, FormDescription components using react-hook-form's useFormContext)
       - `src/components/ui/slider.tsx` -- shadcn Slider (Radix UI Slider)
       - `src/components/ui/card.tsx` -- shadcn Card (Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter)
       All components must follow existing pattern from button.tsx: data-slot attributes, cn() utility, CVA where applicable.

    3. Create Zustand config store at `src/features/settings/hooks/useConfigStore.ts`:
       - Use `create` from zustand with `persist` middleware
       - Fields (with defaults from RESEARCH.md):
         llmApiKey: '' | llmBaseUrl: 'https://dashscope.aliyuncs.com/compatible-mode/v1' | llmModel: 'qwen-plus'
         boostLlmApiKey: '' | boostLlmBaseUrl: '' | boostLlmModel: ''
         agentCount: 10 | maxRounds: 5 | enableTwitter: true | enableReddit: true
         zepApiKey: '' | zepCloudUrl: 'https://api.getzep.com'
       - Action: setConfig(partial: Partial<ConfigState>) => set(partial)
       - Persist key: 'mirofish-config'
       - Partialize: exclude setConfig function from persistence
       - Export ConfigState type

    4. Create Zod schemas at `src/features/settings/schemas/config.schema.ts`:
       - Import from 'zod/v4' (project convention)
       - apiKeysSchema: { llmApiKey: string min 1, llmBaseUrl: url }
       - modelSchema: { llmModel: string min 1, boostLlmApiKey: optional string, boostLlmBaseUrl: optional url or empty string, boostLlmModel: optional string }
       - simulationSchema: { agentCount: int 2-50, maxRounds: int 1-20, enableTwitter: boolean, enableReddit: boolean } with refine requiring at least one platform enabled
       - zepSchema: { zepApiKey: optional string, zepCloudUrl: optional url or empty string }
       - Export inferred types via z.infer for each schema
       - Validation error messages in Polish (per UI-SPEC copywriting contract)

    5. Create i18n settings namespace:
       - `src/locales/pl/settings.json` -- All Polish strings from UI-SPEC copywriting contract (page title, section headings, field labels, button text, connection test results, empty states, error messages)
       - `src/locales/en/settings.json` -- All English equivalents from UI-SPEC
       - Update `src/i18n/config.ts` to add static imports for pl/settings.json and en/settings.json, add 'settings' to the ns array and resources object

    6. Create unit tests:
       - `tests/features/settings/config-schema.test.ts` -- Test all 4 Zod schemas with valid/invalid inputs:
         apiKeys: valid key+url passes, empty key fails, invalid URL fails
         model: preset name passes, empty model fails
         simulation: agentCount 2-50 range, maxRounds 1-20, at least one platform required
         zep: empty values pass (optional), invalid URL fails
       - `tests/features/settings/config-store.test.ts` -- Test Zustand store:
         initial defaults are correct, setConfig updates values, persist key is 'mirofish-config'
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm test -- --run tests/features/settings/config-schema.test.ts tests/features/settings/config-store.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "react-hook-form" package.json
    - grep -q "@hookform/resolvers" package.json
    - test -f src/components/ui/input.tsx
    - test -f src/components/ui/select.tsx
    - test -f src/components/ui/switch.tsx
    - test -f src/components/ui/label.tsx
    - test -f src/components/ui/form.tsx
    - test -f src/components/ui/slider.tsx
    - test -f src/components/ui/card.tsx
    - grep -q "useConfigStore" src/features/settings/hooks/useConfigStore.ts
    - grep -q "mirofish-config" src/features/settings/hooks/useConfigStore.ts
    - grep -q "apiKeysSchema" src/features/settings/schemas/config.schema.ts
    - grep -q "simulationSchema" src/features/settings/schemas/config.schema.ts
    - grep -q "settings" src/i18n/config.ts
    - test -f src/locales/pl/settings.json
    - test -f src/locales/en/settings.json
  </acceptance_criteria>
  <done>
    react-hook-form and @hookform/resolvers installed. 7 shadcn components created. Zustand config store with persist middleware exports useConfigStore. 4 Zod schemas validate all config sections. i18n settings namespace registered with Polish and English translations. All schema and store tests pass. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SettingsPage shell with SectionNav and 3 config sections (API Keys, Model, Simulation)</name>
  <files>
    src/features/settings/SettingsPage.tsx,
    src/features/settings/components/SectionNav.tsx,
    src/features/settings/components/ApiKeysSection.tsx,
    src/features/settings/components/ModelSection.tsx,
    src/features/settings/components/SimulationSection.tsx
  </files>
  <read_first>
    src/features/settings/hooks/useConfigStore.ts (store API from Task 1),
    src/features/settings/schemas/config.schema.ts (Zod schemas from Task 1),
    src/components/ui/form.tsx (Form component API from Task 1),
    src/components/ui/input.tsx (Input component from Task 1),
    src/components/ui/select.tsx (Select component from Task 1),
    src/components/ui/slider.tsx (Slider component from Task 1),
    src/components/ui/switch.tsx (Switch component from Task 1),
    src/components/ui/card.tsx (Card component from Task 1),
    src/locales/pl/settings.json (i18n keys from Task 1),
    .planning/phases/03-configuration-panel/03-UI-SPEC.md (layout contract, section details, interaction patterns)
  </read_first>
  <action>
    1. Replace `src/features/settings/SettingsPage.tsx` placeholder with full settings page:
       - Page title "Ustawienia" (Display: 30px semibold) + subtitle from UI-SPEC
       - Two-column layout inside content area: SectionNav (200px sticky) + config sections (max-width 720px)
       - Sections stacked vertically with 48px gap between them
       - Each section gets an `id` attribute for anchor-link navigation
       - Content padding: 32px
       - On tablet (< 1280px), hide SectionNav, full-width sections

    2. Create `src/features/settings/components/SectionNav.tsx`:
       - Sticky sidebar at top: 88px (header 56px + 32px padding)
       - 5 nav items with Lucide icons: Key (Klucze API), Bot (Wybor modelu), Sliders (Parametry symulacji), Cloud (Zep Cloud), Container (Docker)
       - Active item: accent text + 2px accent left border
       - IntersectionObserver on section headings to track active section
       - Click handler: smooth-scroll to section, update URL hash
       - Typography: Label (13px semibold), item padding 8px vertical 12px horizontal
       - Uses useTranslation('settings') for nav item labels

    3. Create `src/features/settings/components/ApiKeysSection.tsx`:
       - Section with id="api-keys", Key icon + heading "Klucze API"
       - React Hook Form with zodResolver(apiKeysSchema), mode: 'onBlur'
       - defaultValues from useConfigStore
       - Fields: LLM API Key (Input, monospace font, full-width), LLM Base URL (Input type url)
       - Auto-save pattern: useEffect watching form values with 500ms debounce. On valid change, call configStore.setConfig(values) and show toast.success(t('settings:saved'), { duration: 1500 })
       - Inline "Testuj" button next to API key field (accent variant)
       - Connection test via useMutation: POST to `${llmBaseUrl}/chat/completions` with model and messages=[{role:'user',content:'Say "ok"'}], max_tokens:5
       - CORS mitigation: route through Vite proxy if needed. For v1, attempt direct fetch; if CORS fails, show specific error message
       - Test result: green CheckCircle + "Polaczono" on success, red XCircle + error message on failure
       - Loading state: spinner replaces button text during test
       - Cleanup debounce on unmount

    4. Create `src/features/settings/components/ModelSection.tsx`:
       - Section with id="model-selection", Bot icon + heading "Wybor modelu"
       - React Hook Form with zodResolver(modelSchema), mode: 'onBlur'
       - Model Select with options: qwen-plus, gpt-4, custom ("Wlasny...")
       - When "Wlasny" selected, show text Input for custom model name
       - Separator between main model and boost model fields
       - Boost fields (optional): API Key, Base URL, Model -- labeled "(opcjonalnie)"
       - Same auto-save pattern as ApiKeysSection

    5. Create `src/features/settings/components/SimulationSection.tsx`:
       - Section with id="simulation", Sliders icon + heading "Parametry symulacji"
       - React Hook Form with zodResolver(simulationSchema), mode: 'onBlur'
       - Agent count: Slider 2-50, default 10, current value shown as Badge (accent variant)
       - Max rounds: Slider 1-20, default 5, current value shown as Badge
       - Platform toggles: Switch + Label for Twitter and Reddit, horizontal row 24px gap
       - Validation: "Wybierz co najmniej jedna platforme" error below switches if both disabled
       - Same auto-save pattern

    All sections use Card component as container (white background, 1px border, 8px radius, 24px padding).
    All sections use useTranslation('settings') for labels and messages.
    Section heading pattern: flex items-center gap-2, icon 20px h-5 w-5 text-muted-foreground, heading text-[22px] font-semibold.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "SectionNav" src/features/settings/SettingsPage.tsx
    - grep -q "ApiKeysSection" src/features/settings/SettingsPage.tsx
    - grep -q "ModelSection" src/features/settings/SettingsPage.tsx
    - grep -q "SimulationSection" src/features/settings/SettingsPage.tsx
    - grep -q "IntersectionObserver" src/features/settings/components/SectionNav.tsx
    - grep -q "useConfigStore" src/features/settings/components/ApiKeysSection.tsx
    - grep -q "zodResolver" src/features/settings/components/ApiKeysSection.tsx
    - grep -q "useMutation" src/features/settings/components/ApiKeysSection.tsx
    - grep -q "Testuj" src/locales/pl/settings.json
    - grep -q "qwen-plus" src/features/settings/components/ModelSection.tsx
    - grep -q "Slider" src/features/settings/components/SimulationSection.tsx
    - grep -q "Switch" src/features/settings/components/SimulationSection.tsx
  </acceptance_criteria>
  <done>
    SettingsPage displays scrollable single-page layout with sticky SectionNav sidebar showing 5 section links with scroll-spy active highlighting. API Keys section has monospace inputs for key/URL, inline "Testuj" button that sends LLM test request showing success/failure inline. Model section has preset Select (qwen-plus, gpt-4, custom) with conditional custom input and optional boost fields. Simulation section has sliders for agent count (2-50) and rounds (1-20) with value badges, plus Twitter/Reddit platform toggles with validation. All sections auto-save to localStorage via Zustand persist on valid changes. TypeScript compiles and build succeeds.
  </done>
</task>

</tasks>

<verification>
- `pnpm test -- --run tests/features/settings/` -- all schema and store tests pass
- `pnpm typecheck` -- no TypeScript errors
- `pnpm build` -- production build succeeds
- Settings page renders at /settings route with 3 sections and section navigation
</verification>

<success_criteria>
1. User visits /settings and sees "Ustawienia" page with sticky section navigation and 3 config sections
2. API key and base URL fields validate on blur, "Testuj" button sends LLM test request with inline feedback
3. Model dropdown offers qwen-plus, gpt-4, custom entry with conditional custom input field
4. Simulation sliders adjust agent count (2-50) and rounds (1-20) with current value badges
5. Platform toggle switches enforce at least one platform enabled
6. All changes auto-save to localStorage (key: mirofish-config) after 500ms debounce
7. All text renders in Polish by default, English available via language switch
8. All tests pass and TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-panel/03-01-SUMMARY.md`
</output>
