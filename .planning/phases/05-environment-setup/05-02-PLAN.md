---
phase: 05-environment-setup
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/environment/components/AgentProfileCard.tsx
  - src/features/environment/components/AgentProfileGrid.tsx
  - src/features/environment/components/ParameterSummary.tsx
  - src/features/environment/components/ParameterOverrideForm.tsx
  - src/features/environment/components/LaunchConfirmDialog.tsx
  - src/features/environment/pages/EnvironmentPage.tsx
  - src/features/environment/index.ts
  - tests/features/environment/agent-cards.test.tsx
  - tests/features/environment/parameter-form.test.tsx
autonomous: true
requirements:
  - ENVR-02
  - ENVR-03
  - ENVR-04

must_haves:
  truths:
    - "User can view agent profile cards with name, personality, stance, and Lucide icon avatar"
    - "Agent cards display in a 3-4 column grid on desktop"
    - "User can see a summary card showing current simulation parameters"
    - "User can click Dostosuj to open inline parameter override form"
    - "Parameter overrides do NOT persist to global config store"
    - "Tab-based layout with Encje, Agenci, and Parametry tabs"
    - "Uruchom symulacje CTA button visible on all tabs"
    - "Confirmation dialog shows parameter summary before launching"
    - "Auto-create and auto-prepare simulation on page mount"
  artifacts:
    - path: "src/features/environment/components/AgentProfileCard.tsx"
      provides: "Single agent profile card with Lucide icon"
      contains: "AgentProfileCard"
    - path: "src/features/environment/components/AgentProfileGrid.tsx"
      provides: "Grid layout for agent cards"
      contains: "AgentProfileGrid"
    - path: "src/features/environment/components/ParameterSummary.tsx"
      provides: "Summary card with current values"
      contains: "ParameterSummary"
    - path: "src/features/environment/components/ParameterOverrideForm.tsx"
      provides: "Inline slider/switch form for per-simulation override"
      contains: "ParameterOverrideForm"
    - path: "src/features/environment/components/LaunchConfirmDialog.tsx"
      provides: "AlertDialog confirmation before simulation start"
      contains: "LaunchConfirmDialog"
    - path: "src/features/environment/pages/EnvironmentPage.tsx"
      provides: "Tab-based page composing all environment components"
      contains: "EnvironmentPage"
  key_links:
    - from: "src/features/environment/pages/EnvironmentPage.tsx"
      to: "src/features/environment/hooks/useEnvironmentStore.ts"
      via: "store for step tracking"
      pattern: "useEnvironmentStore"
    - from: "src/features/environment/pages/EnvironmentPage.tsx"
      to: "src/api/simulation.ts"
      via: "create + prepare on mount"
      pattern: "simulationApi\\.(create|prepare)"
    - from: "src/features/environment/components/ParameterOverrideForm.tsx"
      to: "src/features/settings/hooks/useConfigStore.ts"
      via: "read defaults only, no write-back"
      pattern: "useConfigStore"
    - from: "src/features/environment/components/LaunchConfirmDialog.tsx"
      to: "src/api/simulation.ts"
      via: "simulationApi.start"
      pattern: "simulationApi\\.start"
---

<objective>
Build agent profile cards, parameter override form, launch confirmation dialog, and compose the tab-based EnvironmentPage with auto-prepare flow.

Purpose: Completes pipeline stage 2 (ENVR-02, ENVR-03, ENVR-04) -- users review agents, adjust parameters, and launch simulation.
Output: Fully functional /environment page with three tabs, agent cards, parameter override, and simulation launch flow.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-environment-setup/05-CONTEXT.md
@.planning/phases/05-environment-setup/05-RESEARCH.md
@.planning/phases/05-environment-setup/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/features/environment/hooks/useEnvironmentStore.ts:
```typescript
export type EnvironmentStep = 'loading' | 'review' | 'preparing' | 'ready'
export interface EnvironmentState {
  step: EnvironmentStep
  simulationId: string | null
  prepareTaskId: string | null
  entityTypes: string[]
  setStep: (step: EnvironmentStep) => void
  setSimulationId: (id: string | null) => void
  setPrepareTaskId: (id: string | null) => void
  setEntityTypes: (types: string[]) => void
  reset: () => void
}
export const useEnvironmentStore: UseBoundStore<StoreApi<EnvironmentState>>
```

From src/features/environment/hooks/useEntities.ts:
```typescript
export function useEntities(graphId: string | null, entityTypeFilter?: string): UseQueryResult
// Returns: { entities, entityTypes, count }
```

From src/features/environment/hooks/useAgentProfiles.ts:
```typescript
export function useAgentProfiles(simulationId: string | null): UseQueryResult
// Returns: { profiles, count }
```

From src/features/environment/hooks/usePrepareStatus.ts:
```typescript
export function usePrepareStatus(taskId: string | null): UseQueryResult
// Polls at 2s interval, stops when status is 'completed' or 'failed'
```

From src/features/environment/components/EntityTable.tsx:
```typescript
export function EntityTable(props: { entities: Entity[]; entityTypes: string[]; isLoading: boolean; relationshipCounts?: Record<string, number> }): JSX.Element
```

From src/features/settings/schemas/config.schema.ts:
```typescript
export const simulationSchema = z.object({ agentCount: z.number().int().min(2).max(50), maxRounds: z.number().int().min(1).max(20), enableTwitter: z.boolean(), enableReddit: z.boolean() }).refine(...)
export type SimulationFormValues = z.infer<typeof simulationSchema>
```

From src/features/settings/hooks/useConfigStore.ts:
```typescript
export interface ConfigState { agentCount: number; maxRounds: number; enableTwitter: boolean; enableReddit: boolean; /* ... */ }
```

From src/api/schemas/simulation.ts:
```typescript
export const ProfileSchema = z.object({ agent_id: z.string(), name: z.string(), personality: z.string().optional(), stance: z.string().optional(), platform: z.string().optional() })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent profile cards and grid</name>
  <files>
    src/features/environment/components/AgentProfileCard.tsx
    src/features/environment/components/AgentProfileGrid.tsx
    tests/features/environment/agent-cards.test.tsx
  </files>
  <read_first>
    src/api/schemas/simulation.ts
    src/features/environment/hooks/useAgentProfiles.ts
    src/locales/pl/environment.json
    src/components/ui/card.tsx
  </read_first>
  <action>
    1. Create `src/features/environment/components/AgentProfileCard.tsx`:
       - Props: profile (inferred from ProfileSchema -- agent_id, name, personality?, stance?, platform?)
       - Read-only card -- per user locked decision (agents generated by backend, not editable)
       - Avatar: rounded-full bg-muted container with Lucide icon based on platform or agent type
         - Icon mapping: { twitter: Twitter icon (Bird or similar), reddit: MessageCircle, default: User } from lucide-react
         - If no platform, use User icon as fallback
       - Card content sections:
         - Name as CardTitle (text-base)
         - Personality: label "Osobowosc" (from i18n) + personality text, show only if field exists
         - Stance: label "Stanowisko" (from i18n) + stance text, show only if field exists
         - Platform: small Badge showing platform name, show only if field exists
       - Use Card, CardHeader, CardContent from shadcn
       - data-testid="agent-card" on Card, data-testid="agent-name" on name

    2. Create `src/features/environment/components/AgentProfileGrid.tsx`:
       - Props: profiles (array), isLoading (boolean), isPreparing (boolean)
       - Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4` -- 3-4 columns on desktop per user decision
       - Loading state (isPreparing=true): show LoadingSpinner with text from i18n "Generowanie profili agentow..."
       - Empty state (no profiles, not preparing): show message "Profile zostana wygenerowane po uruchomieniu" from i18n
       - Maps profiles to AgentProfileCard components
       - data-testid="agent-grid"

    3. Create `tests/features/environment/agent-cards.test.tsx`:
       - Test: AgentProfileCard renders name
       - Test: AgentProfileCard renders personality when present
       - Test: AgentProfileCard renders stance when present
       - Test: AgentProfileCard handles missing optional fields gracefully
       - Test: AgentProfileGrid renders cards for each profile
       - Test: AgentProfileGrid shows loading state when isPreparing
       - Test: AgentProfileGrid shows empty message when no profiles and not preparing
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && npx vitest run tests/features/environment/agent-cards.test.tsx -x && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AgentProfileCard" src/features/environment/components/AgentProfileCard.tsx
    - grep -q "grid-cols-3" src/features/environment/components/AgentProfileGrid.tsx
    - grep -q "grid-cols-4" src/features/environment/components/AgentProfileGrid.tsx
    - grep -q "agent-card" tests/features/environment/agent-cards.test.tsx
  </acceptance_criteria>
  <done>Agent profile cards render with name, personality, stance, platform badge, and Lucide icon avatar. Grid displays 3-4 columns on desktop. Loading and empty states work. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Parameter form, launch dialog, and EnvironmentPage composition</name>
  <files>
    src/features/environment/components/ParameterSummary.tsx
    src/features/environment/components/ParameterOverrideForm.tsx
    src/features/environment/components/LaunchConfirmDialog.tsx
    src/features/environment/pages/EnvironmentPage.tsx
    src/features/environment/index.ts
    tests/features/environment/parameter-form.test.tsx
  </files>
  <read_first>
    src/features/settings/components/SimulationSection.tsx
    src/features/settings/schemas/config.schema.ts
    src/features/settings/hooks/useConfigStore.ts
    src/features/environment/hooks/useEnvironmentStore.ts
    src/features/environment/hooks/useEntities.ts
    src/features/environment/hooks/useAgentProfiles.ts
    src/features/environment/hooks/usePrepareStatus.ts
    src/features/environment/components/EntityTable.tsx
    src/features/environment/components/AgentProfileGrid.tsx
    src/features/graph/hooks/useGraphStore.ts
    src/components/ui/tabs.tsx
    src/components/ui/alert-dialog.tsx
    src/locales/pl/environment.json
    src/app/App.tsx
  </read_first>
  <action>
    1. Create `src/features/environment/components/ParameterSummary.tsx`:
       - Props: values ({ agentCount, maxRounds, enableTwitter, enableReddit }), onEdit (() => void)
       - Card with flex layout: left side shows "Agenci: {N}, Rundy: {N}, Platformy: {platforms}" -- per user decision
       - Platforms: join enabled platforms with " + " (e.g., "Twitter + Reddit")
       - Right side: Button variant="outline" size="sm" with text "Dostosuj" from i18n
       - Use useTranslation('environment')

    2. Create `src/features/environment/components/ParameterOverrideForm.tsx`:
       - Props: defaultValues (SimulationFormValues), onValuesChange ((values: SimulationFormValues) => void), visible (boolean)
       - CRITICAL: This form reads config store defaults but does NOT write back to config store -- per user locked decision (per-simulation only)
       - Use react-hook-form + zodResolver with simulationSchema from settings/schemas/config.schema.ts
       - defaultValues come from props (which read from useConfigStore in parent)
       - Same slider/switch layout as SimulationSection.tsx but WITHOUT auto-save debounce
       - Instead, call onValuesChange whenever form values change (parent tracks current values for launch)
       - Show/hide based on `visible` prop (toggled by ParameterSummary "Dostosuj" button)
       - Fields: agentCount (Slider 2-50), maxRounds (Slider 1-20), enableTwitter (Switch), enableReddit (Switch)
       - Platform validation: at least one platform enabled (same refine rule)

    3. Create `src/features/environment/components/LaunchConfirmDialog.tsx`:
       - Props: params (SimulationFormValues), onConfirm (() => void), disabled (boolean)
       - AlertDialog wrapping the CTA button
       - Trigger: Button size="lg" with text "Uruchom symulacje" from i18n -- per user decision this is a CTA at the bottom
       - Content: AlertDialogTitle "Potwierdzenie uruchomienia", AlertDialogDescription showing parameter summary (Agenci: N, Rundy: N, Platformy: ...)
       - Footer: AlertDialogCancel "Anuluj" + AlertDialogAction "Uruchom" calling onConfirm
       - Button disabled when `disabled` prop is true (e.g., still preparing)

    4. Create `src/features/environment/pages/EnvironmentPage.tsx`:
       - Tab-based layout with three tabs: Encje | Agenci | Parametry -- per user locked decision
       - NOT a scrollable single page -- each tab focuses on one concern
       - Route guard: check useGraphStore().step === 'done' && useGraphStore().graphId !== null; if not, redirect to /graph with toast message

       - **Auto-prepare flow on mount:**
         a. On mount (useEffect), if environment store has no simulationId:
            - Call simulationApi.create({ project_id: graphStore.projectId, graph_id: graphStore.graphId })
            - Store simulation_id in environment store
            - Call simulationApi.prepare({ simulation_id, entity_types: entityData.entityTypes, use_llm_for_profiles: true })
            - Store task_id in environment store, set step to 'preparing'
         b. usePrepareStatus polls until complete
         c. When prepare completes, set step to 'review', fetch profiles via useAgentProfiles
         d. If simulationId already exists in store (page revisit), skip create/prepare, just fetch

       - **Tab 1 (Encje):** Render EntityTable with useEntities(graphStore.graphId) data
         - For relationship counts: use cached graph data from useGraphData(graphStore.graphId) and count edges per entity
       - **Tab 2 (Agenci):** Render AgentProfileGrid with useAgentProfiles(environmentStore.simulationId) data
         - Show preparing state while prepare is in progress
       - **Tab 3 (Parametry):** Render ParameterSummary + ParameterOverrideForm
         - Local state: paramValues initialized from useConfigStore defaults
         - "Dostosuj" toggles form visibility
         - Form changes update local paramValues (NOT config store)

       - **CTA at bottom (visible on all tabs):** LaunchConfirmDialog with current paramValues
         - onConfirm: call simulationApi.start({ simulation_id, max_rounds: paramValues.maxRounds })
         - After start succeeds: navigate to /simulation (Phase 6 route, may not exist yet -- navigate anyway)
         - disabled when step !== 'review' and step !== 'ready' (still preparing or loading)

       - Use Tabs, TabsList, TabsTrigger, TabsContent from shadcn

    5. Update `src/features/environment/index.ts`: Export EnvironmentPage from pages/EnvironmentPage (replace placeholder from Plan 01).

    6. Create `tests/features/environment/parameter-form.test.tsx`:
       - Test: ParameterSummary renders agent count, round count, platform names
       - Test: ParameterSummary calls onEdit when Dostosuj clicked
       - Test: ParameterOverrideForm renders sliders and switches with default values
       - Test: ParameterOverrideForm does NOT call config store setConfig (verify no auto-save)
       - Test: LaunchConfirmDialog renders CTA button text
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && npx vitest run tests/features/environment/ -x && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ParameterSummary" src/features/environment/components/ParameterSummary.tsx
    - grep -q "Dostosuj" src/features/environment/components/ParameterSummary.tsx || grep -q "parameters.adjust" src/features/environment/components/ParameterSummary.tsx
    - grep -q "ParameterOverrideForm" src/features/environment/components/ParameterOverrideForm.tsx
    - grep -q "LaunchConfirmDialog" src/features/environment/components/LaunchConfirmDialog.tsx
    - grep -q "AlertDialog" src/features/environment/components/LaunchConfirmDialog.tsx
    - grep -q "TabsList" src/features/environment/pages/EnvironmentPage.tsx
    - grep -q "simulationApi" src/features/environment/pages/EnvironmentPage.tsx
    - grep -q "useConfigStore" src/features/environment/components/ParameterOverrideForm.tsx || grep -q "defaultValues" src/features/environment/components/ParameterOverrideForm.tsx
    - grep -rL "setConfig" src/features/environment/components/ParameterOverrideForm.tsx
  </acceptance_criteria>
  <done>EnvironmentPage renders 3 tabs (Encje/Agenci/Parametry). Agent profile cards show in grid. Parameter summary + override form works without persisting to global config. Launch confirmation dialog triggers simulation start. Auto-prepare flow creates simulation and generates agents on mount. All tests pass. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/features/environment/ -x` -- all environment tests pass
- `npx tsc --noEmit` -- no type errors
- Navigate to /environment: three tabs render
- Encje tab: entity table with sort and filter
- Agenci tab: agent cards in grid (or loading/empty state)
- Parametry tab: summary card with Dostosuj button, inline override form
- CTA button at bottom, confirmation dialog before launch
</verification>

<success_criteria>
- ENVR-02 implemented: agent profile cards with personality, stance, Lucide icon avatars
- ENVR-03 implemented: parameter summary + override form (per-simulation only, NOT persisted)
- ENVR-04 implemented: agent cards with personality traits, memory state, stance display
- Tab-based layout (Encje | Agenci | Parametry) per user decision
- Launch confirmation dialog with parameter summary
- Auto-prepare flow on page mount
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-environment-setup/05-02-SUMMARY.md`
</output>
