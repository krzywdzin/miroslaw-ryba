---
phase: 09-polish-and-differentiation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/hooks/useGlobalShortcuts.ts
  - src/hooks/useGlobalShortcuts.test.ts
  - src/components/shared/ShortcutsHelpModal.tsx
  - src/components/shared/ShortcutsHelpModal.test.tsx
  - src/app/App.tsx
  - src/locales/pl/common.json
  - src/locales/en/common.json
autonomous: true
requirements:
  - UIUX-07

must_haves:
  truths:
    - "User can press 1-5 to navigate between pipeline stages"
    - "User can press / to focus the chat input field"
    - "User can press ? to open a help modal listing all shortcuts"
    - "User can press [ and ] to switch panels/tabs on the current page"
    - "Shortcuts do not fire when user is typing in input, textarea, or select elements"
    - "Help modal shows shortcut descriptions in the current language (PL/EN)"
  artifacts:
    - path: "src/hooks/useGlobalShortcuts.ts"
      provides: "Global keyboard shortcut registrations"
      exports: ["useGlobalShortcuts"]
    - path: "src/components/shared/ShortcutsHelpModal.tsx"
      provides: "Keyboard shortcuts help modal"
      exports: ["ShortcutsHelpModal"]
  key_links:
    - from: "src/app/App.tsx"
      to: "src/hooks/useGlobalShortcuts.ts"
      via: "useGlobalShortcuts called in ShortcutProvider wrapper"
      pattern: "useGlobalShortcuts"
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "react-hotkeys-hook"
      via: "useHotkeys import"
      pattern: "import.*useHotkeys.*from.*react-hotkeys-hook"
    - from: "src/hooks/useGlobalShortcuts.ts"
      to: "src/components/shared/ShortcutsHelpModal.tsx"
      via: "help modal state management"
      pattern: "ShortcutsHelpModal|helpOpen"
---

<objective>
Implement keyboard shortcuts for pipeline navigation (1-5), chat focus (/), panel switching ([ ]), and help modal (?), with automatic input-element scoping to prevent conflicts with typing.

Purpose: Power users can navigate the app without mouse, fulfilling UIUX-07.
Output: Working keyboard shortcuts with help modal, react-hotkeys-hook integration.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-differentiation/09-CONTEXT.md
@.planning/phases/09-polish-and-differentiation/09-RESEARCH.md
@.planning/phases/09-polish-and-differentiation/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/App.tsx:
```typescript
// createBrowserRouter with routes:
// / (index) -> DashboardPage
// /graph -> GraphUploadPage
// /graph/view -> GraphViewPage
// /environment -> EnvironmentPage
// /simulation -> SimulationPage
// /report/:simulationId -> ReportPage
// /history -> HistoryPage
// /chat/:simulationId -> ChatPage
// /settings -> SettingsPage
export default function App() {
  return <RouterProvider router={router} />
}
```

From src/components/layout/AppShell.tsx:
```typescript
// Renders Header, Sidebar, main with Outlet, Footer
// This is where useGlobalShortcuts should be called (inside RouterProvider context)
export function AppShell() { ... }
```

From src/components/ui/dialog.tsx (already installed shadcn Dialog):
```typescript
export { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogTrigger, DialogClose }
```

Route mapping for shortcuts:
- Key 1 -> /graph
- Key 2 -> /environment
- Key 3 -> /simulation
- Key 4 -> /history
- Key 5 -> /chat (note: chat requires simulationId, navigate to base /chat or latest)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install react-hotkeys-hook, create useGlobalShortcuts hook and ShortcutsHelpModal</name>
  <files>package.json, src/hooks/useGlobalShortcuts.ts, src/hooks/useGlobalShortcuts.test.ts, src/components/shared/ShortcutsHelpModal.tsx, src/components/shared/ShortcutsHelpModal.test.tsx, src/locales/pl/common.json, src/locales/en/common.json</files>
  <read_first>
    - src/app/App.tsx
    - src/components/layout/AppShell.tsx
    - src/locales/pl/common.json
    - src/locales/en/common.json
    - .planning/phases/09-polish-and-differentiation/09-RESEARCH.md (Pattern 3 section)
    - .planning/phases/09-polish-and-differentiation/09-UI-SPEC.md (Keyboard Shortcuts Contract, Help Modal, Copywriting Contract)
  </read_first>
  <behavior>
    - useGlobalShortcuts returns { helpOpen, setHelpOpen }
    - Pressing "1" calls navigate("/graph")
    - Pressing "2" calls navigate("/environment")
    - Pressing "3" calls navigate("/simulation")
    - Pressing "4" calls navigate("/history")
    - Pressing "5" calls navigate("/chat")
    - Pressing "/" prevents default and focuses element with [data-chat-input] attribute
    - Pressing "?" (or shift+/) sets helpOpen to true
    - Pressing "[" dispatches a custom event "panel-prev" on window
    - Pressing "]" dispatches a custom event "panel-next" on window
    - None of the above fire when focus is inside input, textarea, select, or contenteditable
    - ShortcutsHelpModal renders a Dialog with shortcut list
  </behavior>
  <action>
    1. Install react-hotkeys-hook:
       ```bash
       pnpm add react-hotkeys-hook
       ```

    2. Add i18n strings to both locale files.
       Polish (common.json) -- add these keys:
       ```json
       "shortcuts": {
         "title": "Skróty klawiszowe",
         "navigate": "Nawigacja między etapami",
         "chatFocus": "Fokus na pole czatu",
         "panelSwitch": "Przełączanie paneli",
         "help": "Pomoc (skróty)"
       }
       ```
       English (common.json) -- add these keys:
       ```json
       "shortcuts": {
         "title": "Keyboard Shortcuts",
         "navigate": "Navigate between stages",
         "chatFocus": "Focus chat input",
         "panelSwitch": "Switch panels",
         "help": "Help (shortcuts)"
       }
       ```

       IMPORTANT: If Plan 01 has already added `"theme"` keys to these files, preserve them. Merge, do not overwrite.

    3. Create `src/hooks/useGlobalShortcuts.ts`:
       - Import useHotkeys from 'react-hotkeys-hook'
       - Import useNavigate from 'react-router'
       - Import useState
       - Manage helpOpen state with useState(false)
       - Register shortcuts with useHotkeys:
         - `'1'` -> navigate('/graph'), { enableOnFormTags: false }
         - `'2'` -> navigate('/environment'), { enableOnFormTags: false }
         - `'3'` -> navigate('/simulation'), { enableOnFormTags: false }
         - `'4'` -> navigate('/history'), { enableOnFormTags: false }
         - `'5'` -> navigate('/chat'), { enableOnFormTags: false }
         - `'/'` -> e.preventDefault(), find [data-chat-input] and focus(), { enableOnFormTags: false }
         - `'?'` or `'shift+/'` -> setHelpOpen(true), { enableOnFormTags: false }
         - `'['` -> window.dispatchEvent(new CustomEvent('panel-prev')), { enableOnFormTags: false }
         - `']'` -> window.dispatchEvent(new CustomEvent('panel-next')), { enableOnFormTags: false }
       - Return { helpOpen, setHelpOpen }
       - Note on `?` key: Try binding `'?'` first. If that does not work during testing, use `'shift+/'` instead. react-hotkeys-hook v5 may support `?` directly.

    4. Create `src/components/shared/ShortcutsHelpModal.tsx`:
       - Import Dialog, DialogContent, DialogHeader, DialogTitle from '@/components/ui/dialog'
       - Import useTranslation from 'react-i18next'
       - Props: { open: boolean; onOpenChange: (open: boolean) => void }
       - Define shortcuts array:
         - { keys: '1-5', description: t('shortcuts.navigate') }
         - { keys: '/', description: t('shortcuts.chatFocus') }
         - { keys: '[ ]', description: t('shortcuts.panelSwitch') }
         - { keys: '?', description: t('shortcuts.help') }
       - Render Dialog with open/onOpenChange props
       - DialogTitle: t('shortcuts.title')
       - Map shortcuts to rows: left side text-sm text-muted-foreground, right side kbd element with `rounded bg-muted px-2 py-1 font-mono text-xs`

    5. Create `src/hooks/useGlobalShortcuts.test.ts`:
       - Mock react-router's useNavigate
       - Mock react-hotkeys-hook's useHotkeys to capture callback registrations
       - Test: registers hotkey for each key (1-5, /, ?, [, ])
       - Test: navigate called with correct path for keys 1-5
       - Test: all hotkeys use enableOnFormTags: false

    6. Create `src/components/shared/ShortcutsHelpModal.test.tsx`:
       - Test: renders dialog with title when open=true
       - Test: renders all 4 shortcut entries
       - Test: kbd elements display correct key labels
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm exec vitest run src/hooks/useGlobalShortcuts.test.ts src/components/shared/ShortcutsHelpModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react-hotkeys-hook" in dependencies
    - src/hooks/useGlobalShortcuts.ts exports useGlobalShortcuts
    - src/hooks/useGlobalShortcuts.ts contains `enableOnFormTags: false` for each binding
    - src/components/shared/ShortcutsHelpModal.tsx exports ShortcutsHelpModal
    - ShortcutsHelpModal renders 4 shortcut entries with kbd elements
    - pl/common.json contains `"shortcuts"` key with `"title"` value `"Skróty klawiszowe"`
    - en/common.json contains `"shortcuts"` key with `"title"` value `"Keyboard Shortcuts"`
    - All tests pass
  </acceptance_criteria>
  <done>react-hotkeys-hook installed, useGlobalShortcuts hook registers all shortcuts with input guarding, ShortcutsHelpModal displays shortcuts list, i18n strings in both languages</done>
</task>

<task type="auto">
  <name>Task 2: Wire shortcuts into AppShell and add data-chat-input attribute</name>
  <files>src/components/layout/AppShell.tsx, src/features/chat/components/ChatInput.tsx</files>
  <read_first>
    - src/components/layout/AppShell.tsx
    - src/hooks/useGlobalShortcuts.ts (created in Task 1)
    - src/components/shared/ShortcutsHelpModal.tsx (created in Task 1)
    - src/features/chat/components/ChatInput.tsx
  </read_first>
  <action>
    1. Update `src/components/layout/AppShell.tsx`:
       - Import useGlobalShortcuts from '@/hooks/useGlobalShortcuts'
       - Import ShortcutsHelpModal from '@/components/shared/ShortcutsHelpModal'
       - Call `const { helpOpen, setHelpOpen } = useGlobalShortcuts()` inside AppShell
       - Render `<ShortcutsHelpModal open={helpOpen} onOpenChange={setHelpOpen} />` at the end, before the closing div
       - This works because AppShell is rendered inside RouterProvider, so useNavigate is available

    2. Update `src/features/chat/components/ChatInput.tsx` (or whatever file contains the chat input textarea):
       - Find the textarea or input element used for chat message input
       - Add `data-chat-input` attribute to it so the `/` shortcut can find and focus it
       - If ChatInput.tsx does not exist at that path, search for the chat input component and add the attribute there

    3. Run full test suite and typecheck to ensure nothing is broken.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm exec vitest run && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx imports and calls useGlobalShortcuts
    - AppShell.tsx renders ShortcutsHelpModal
    - Chat input element has `data-chat-input` attribute (grep for `data-chat-input` in src/features/chat/)
    - All tests pass, TypeScript compiles
  </acceptance_criteria>
  <done>Keyboard shortcuts are active app-wide via AppShell, ? opens help modal, / focuses chat input via data-chat-input attribute, all shortcuts disabled in form elements</done>
</task>

</tasks>

<verification>
1. `pnpm exec vitest run` -- all tests pass
2. `pnpm exec tsc --noEmit` -- no type errors
3. `grep -rn "useGlobalShortcuts" src/components/layout/AppShell.tsx` -- hook called
4. `grep -rn "ShortcutsHelpModal" src/components/layout/AppShell.tsx` -- modal rendered
5. `grep -rn "data-chat-input" src/features/chat/` -- attribute present on chat input
6. `grep -rn "enableOnFormTags" src/hooks/useGlobalShortcuts.ts` -- input guarding present
7. `grep -n "react-hotkeys-hook" package.json` -- dependency installed
</verification>

<success_criteria>
- Keys 1-5 navigate to correct pipeline stages
- / focuses chat input field
- ? opens help modal with all shortcuts listed
- [ and ] dispatch panel navigation events
- No shortcuts fire when typing in text inputs
- Help modal shows localized descriptions (PL/EN)
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-differentiation/09-02-SUMMARY.md`
</output>
