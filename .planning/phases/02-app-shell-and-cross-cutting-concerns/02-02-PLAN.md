---
phase: 02-app-shell-and-cross-cutting-concerns
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/App.tsx
  - src/app/main.tsx
  - src/app/providers.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/SidebarMobile.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Stepper.tsx
  - src/components/layout/StepperItem.tsx
  - src/components/layout/LanguageSwitcher.tsx
  - src/components/layout/BackendStatus.tsx
  - src/features/dashboard/DashboardPage.tsx
  - src/features/dashboard/components/SystemStatus.tsx
  - src/features/dashboard/components/EmptyState.tsx
  - src/features/settings/SettingsPage.tsx
  - src/features/errors/NotFoundPage.tsx
  - src/hooks/useBackendStatus.ts
  - tests/components/layout.test.tsx
autonomous: true
requirements:
  - UIUX-02
  - UIUX-03
  - I18N-03

must_haves:
  truths:
    - "User sees a Polish-language app shell with header, sidebar stepper, and content area"
    - "User can navigate to dashboard, see 5 pipeline stages in sidebar"
    - "User can switch language between Polish and English via header dropdown"
    - "Sidebar is fixed 280px on desktop, collapses to drawer on tablet"
    - "Dashboard shows welcome heading, system status, empty state, and quick-start button"
    - "Footer shows version, GitHub link, and AGPL note"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Layout route: header + sidebar + outlet + footer"
      min_lines: 15
    - path: "src/components/layout/Header.tsx"
      provides: "Header with logo, breadcrumb, language switcher, config link, backend status"
      min_lines: 20
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Desktop fixed sidebar with 5-stage stepper"
      min_lines: 20
    - path: "src/components/layout/Stepper.tsx"
      provides: "5-stage pipeline stepper with status indicators"
      min_lines: 30
    - path: "src/features/dashboard/DashboardPage.tsx"
      provides: "Dashboard landing page with hero, status, empty state, CTA"
      min_lines: 20
    - path: "src/app/App.tsx"
      provides: "React Router with createBrowserRouter, layout route wrapping all pages"
      exports: ["default"]
  key_links:
    - from: "src/app/App.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "React Router layout route element"
      pattern: "element.*AppShell"
    - from: "src/components/layout/AppShell.tsx"
      to: "react-router Outlet"
      via: "Outlet component renders child routes"
      pattern: "<Outlet"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/LanguageSwitcher.tsx"
      via: "component composition"
      pattern: "<LanguageSwitcher"
    - from: "src/components/layout/LanguageSwitcher.tsx"
      to: "i18next.changeLanguage"
      via: "onClick handler"
      pattern: "changeLanguage"
    - from: "src/components/layout/Stepper.tsx"
      to: "src/locales/*/navigation.json"
      via: "useTranslation('navigation')"
      pattern: "useTranslation.*navigation"
---

<objective>
Build the complete app shell layout: React Router with layout route, header (logo, breadcrumb, language switcher, config button, backend status dot), sidebar with 5-stage stepper, footer, dashboard landing page, and placeholder settings page.

Purpose: Create the visual frame that all pipeline features plug into. After this plan, the app has a real navigable UI in Polish with a working language switcher.

Output: Navigable app shell with header, sidebar stepper, dashboard, footer, and language switching.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-RESEARCH.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-UI-SPEC.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-CONTEXT.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs the executor will use -->

From src/i18n/config.ts (created in Plan 01):
```typescript
// i18next initialized with Polish default, English fallback
// Namespaces: common, navigation, dashboard, errors
// Usage: import { useTranslation } from 'react-i18next'
//        const { t } = useTranslation('navigation')
//        t('stage1') => "Budowa grafu"
```

From src/lib/utils.ts (created in Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/styles/globals.css (created in Plan 01):
```css
/* CSS variables available:
   --header-height: 56px
   --footer-height: 40px
   --sidebar-width: 280px
   --sidebar-width-collapsed: 64px
   --content-max-width: 960px
   --primary: hsl(217 91% 60%)  (blue accent)
   --success: hsl(142 71% 45%)  (green for connected/completed)
   --destructive: hsl(0 84% 60%)  (red for errors)
   --muted: hsl(240 5% 96%)  (sidebar bg, footer bg)
   --muted-foreground: hsl(240 4% 46%)  (disabled text)
*/
```

From src/components/ui/ (shadcn components from Plan 01):
```typescript
// Available: Button, Skeleton, Badge, Separator, Tooltip, Breadcrumb,
//            Collapsible, Sheet, DropdownMenu, Alert, Toaster (sonner)
```

From src/api/client.ts (Phase 1):
```typescript
export async function apiRequest<T>(path: string, options?: ApiRequestOptions): Promise<T>
```

From src/app/providers.tsx (Phase 1):
```typescript
export function Providers({ children }: { children: ReactNode }): JSX.Element
// Wraps QueryClientProvider
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App shell layout, router, header, sidebar, stepper, and footer</name>
  <files>
    src/app/App.tsx, src/app/main.tsx, src/app/providers.tsx,
    src/components/layout/AppShell.tsx, src/components/layout/Header.tsx,
    src/components/layout/Sidebar.tsx, src/components/layout/SidebarMobile.tsx,
    src/components/layout/Footer.tsx, src/components/layout/Stepper.tsx,
    src/components/layout/StepperItem.tsx, src/components/layout/LanguageSwitcher.tsx,
    src/components/layout/BackendStatus.tsx, src/hooks/useBackendStatus.ts,
    src/features/settings/SettingsPage.tsx, src/features/errors/NotFoundPage.tsx
  </files>
  <read_first>
    src/app/App.tsx, src/app/main.tsx, src/app/providers.tsx,
    src/i18n/config.ts, src/lib/utils.ts, src/api/client.ts,
    src/locales/pl/navigation.json, src/locales/pl/common.json
  </read_first>
  <action>
    1. Create `src/hooks/useBackendStatus.ts`:
       - TanStack Query hook: `queryKey: ['backend-status']`, `queryFn` calls `apiRequest('/graph_bp/projects')` (lightest endpoint available), `refetchInterval: 30_000`, `retry: 1`
       - `select: () => true` (if succeeds, backend is connected)
       - Export: `useBackendStatus()` returning `{ isConnected: boolean, isLoading: boolean }`

    2. Create `src/components/layout/BackendStatus.tsx`:
       - 8px circle div, uses `useBackendStatus()` hook
       - Loading: `animate-pulse bg-muted-foreground`
       - Connected: `bg-success` (green)
       - Disconnected: `bg-destructive` (red)
       - Wrapped in Tooltip showing t('common:connected') or t('common:disconnected')

    3. Create `src/components/layout/LanguageSwitcher.tsx`:
       - DropdownMenu with Globe icon (from lucide-react) as trigger button (variant="ghost", size="icon")
       - Two items: "Polski" (code: 'pl') and "English" (code: 'en')
       - onClick calls `i18n.changeLanguage(lang.code)`
       - Active language highlighted with `bg-accent text-accent-foreground`

    4. Create `src/components/layout/StepperItem.tsx`:
       - Props: `{ id: string, label: string, icon: LucideIcon, status: 'completed' | 'current' | 'locked', subSteps?: Array<{ label: string, status: string }> }`
       - Completed: success-colored Check icon, regular weight, clickable
       - Current: accent left-border (3px), semibold label, accent icon color
       - Locked: muted text, Lock icon, `pointer-events-none opacity-50`
       - Sub-steps render inside Collapsible, indented 24px, only expandable when status is 'completed' or 'current'
       - Height: 44px per item, Lucide icons at 18px

    5. Create `src/components/layout/Stepper.tsx`:
       - Uses `useTranslation('navigation')` for stage names
       - Renders 5 StepperItem components with these stages:
         - `{ id: 'graph', label: t('stage1'), icon: Network, status: 'locked' }`
         - `{ id: 'environment', label: t('stage2'), icon: Users, status: 'locked' }`
         - `{ id: 'simulation', label: t('stage3'), icon: Play, status: 'locked' }`
         - `{ id: 'report', label: t('stage4'), icon: FileText, status: 'locked' }`
         - `{ id: 'dialogue', label: t('stage5'), icon: MessageCircle, status: 'locked' }`
       - Props: `stages?: Array<{ id, status }>` to allow overriding status from parent (defaults to all locked for Phase 2)
       - Pure display component -- NO internal useState for active step (per Pitfall 4 from RESEARCH)

    6. Create `src/components/layout/Header.tsx`:
       - Height: 56px, white bg, bottom border 1px `border-border`
       - Left: App name "Miroslaw Ryba" (Geist 15px semibold) using `t('common:appTitle')`
       - Center: Breadcrumb from shadcn showing current page (use react-router `useLocation` to derive)
       - Right group (flex, gap-2, items-center): LanguageSwitcher, Settings icon-button (Settings icon from lucide, navigates to /settings via `useNavigate`), BackendStatus dot
       - Settings button wrapped in Tooltip: `t('common:settings')`

    7. Create `src/components/layout/Sidebar.tsx`:
       - Width: 280px fixed, `bg-muted` background, border-right
       - Padding: 16px
       - Top: "Pipeline" label (13px semibold muted-foreground), then Separator, then Stepper
       - Bottom: settings link with Settings icon, using react-router Link to /settings
       - CSS: `hidden lg:flex flex-col` (hidden on tablet, shown on desktop >= 1280px)

    8. Create `src/components/layout/SidebarMobile.tsx`:
       - Uses shadcn Sheet component, slides from left
       - Trigger: hamburger Menu icon button in header (only shown `lg:hidden`)
       - Content: same as Sidebar but inside Sheet
       - Add the hamburger trigger to Header.tsx (before the logo, only visible `lg:hidden`)

    9. Create `src/components/layout/Footer.tsx`:
       - Height: 40px, `bg-muted` background, border-top
       - Content (flex justify-between, px-6, items-center, text-muted-foreground, text-[13px]):
         - Left: `v${version}` (import version from package.json or hardcode "0.0.1")
         - Center: GitHub link (ExternalLink icon + "GitHub", href to repo URL)
         - Right: `t('common:agplNote')` or hardcode "AGPL-3.0" -- add "agplNote": "Licencja AGPL-3.0" to pl/common.json and "agplNote": "AGPL-3.0 License" to en/common.json if not already present

    10. Create `src/components/layout/AppShell.tsx`:
        - Layout component: flex min-h-screen flex-col
        - Header at top
        - Middle section: flex flex-1, Sidebar (desktop), SidebarMobile (tablet), main content area with `<Outlet />`
        - Main content: `flex-1 p-8` with page transition fade (CSS transition, 150ms ease-out opacity)
        - Footer at bottom
        - Sonner Toaster component at root level: `<Toaster position="bottom-right" toastOptions={{ duration: 5000 }} visibleToasts={3} richColors />`

    11. Create `src/features/settings/SettingsPage.tsx`:
        - Simple placeholder: heading "Ustawienia" / "Settings" via `t('common:settings')`
        - Body text: "Konfiguracja dostepna w Fazie 3" / "Configuration available in Phase 3"
        - Add translation keys to dashboard.json or common.json

    12. Create `src/features/errors/NotFoundPage.tsx`:
        - Centered message: "Nie znaleziono strony" / "Page not found"
        - Link back to home: "Wróć do strony głównej" / "Return to home"

    13. Rewrite `src/app/App.tsx` with React Router:
        ```typescript
        import { createBrowserRouter, RouterProvider } from 'react-router'
        import { AppShell } from '@/components/layout/AppShell'
        import { DashboardPage } from '@/features/dashboard/DashboardPage'
        import { SettingsPage } from '@/features/settings/SettingsPage'
        import { NotFoundPage } from '@/features/errors/NotFoundPage'

        const router = createBrowserRouter([
          {
            path: '/',
            element: <AppShell />,
            errorElement: <NotFoundPage />,
            children: [
              { index: true, element: <DashboardPage /> },
              { path: 'settings', element: <SettingsPage /> },
            ],
          },
        ])

        export default function App() {
          return <RouterProvider router={router} />
        }
        ```

    14. Update `src/app/providers.tsx`:
        - Add TooltipProvider from shadcn wrapping children (needed for all Tooltip usage)
        ```typescript
        import { TooltipProvider } from '@/components/ui/tooltip'
        // Wrap: <TooltipProvider><QueryClientProvider>...</QueryClientProvider></TooltipProvider>
        ```

    IMPORTANT: Import all Lucide icons individually: `import { Network, Users, Play, FileText, MessageCircle, Settings, Globe, Menu, Lock, Check, ExternalLink } from 'lucide-react'`
    IMPORTANT: The Stepper is a PURE DISPLAY component. It accepts status props, does not manage its own state. All 5 stages default to 'locked' in Phase 2.
    IMPORTANT: Sidebar uses `hidden lg:flex` (1280px breakpoint), NOT `hidden md:flex` (768px). Below 1024px, the app shows a "min screen width" message.
    IMPORTANT: All user-visible strings must use `t()` from `useTranslation`. Zero hardcoded strings.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/layout/AppShell.tsx` succeeds
    - `grep -q "Outlet" src/components/layout/AppShell.tsx` succeeds (renders child routes)
    - `grep -q "createBrowserRouter" src/app/App.tsx` succeeds (React Router configured)
    - `test -f src/components/layout/Header.tsx` succeeds
    - `grep -q "LanguageSwitcher" src/components/layout/Header.tsx` succeeds
    - `grep -q "BackendStatus" src/components/layout/Header.tsx` succeeds
    - `test -f src/components/layout/Stepper.tsx` succeeds
    - `grep -q "useTranslation" src/components/layout/Stepper.tsx` succeeds (i18n for stage names)
    - `test -f src/components/layout/Sidebar.tsx` succeeds
    - `grep -q "Sheet" src/components/layout/SidebarMobile.tsx` succeeds (mobile drawer)
    - `test -f src/components/layout/Footer.tsx` succeeds
    - `grep -q "changeLanguage" src/components/layout/LanguageSwitcher.tsx` succeeds
    - `test -f src/features/settings/SettingsPage.tsx` succeeds
    - `test -f src/features/errors/NotFoundPage.tsx` succeeds
    - `grep -q "useBackendStatus" src/hooks/useBackendStatus.ts` succeeds
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    App shell layout renders with header (logo, breadcrumb, language switcher, config button, backend status dot), fixed sidebar with 5-stage stepper, main content area with Outlet, and footer. React Router configured with dashboard (index), settings, and 404 routes. Language switcher changes all UI text between Polish and English.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page with hero, system status, empty state, and CTA</name>
  <files>
    src/features/dashboard/DashboardPage.tsx,
    src/features/dashboard/components/SystemStatus.tsx,
    src/features/dashboard/components/EmptyState.tsx,
    tests/components/layout.test.tsx
  </files>
  <read_first>
    src/components/layout/AppShell.tsx, src/hooks/useBackendStatus.ts,
    src/locales/pl/dashboard.json, src/locales/en/dashboard.json,
    src/components/ui/button.tsx, src/components/ui/badge.tsx
  </read_first>
  <action>
    1. Create `src/features/dashboard/components/SystemStatus.tsx`:
       - Card-like container (rounded-lg border bg-card p-6)
       - Title: `t('dashboard:systemStatus')` in heading typography (22px semibold)
       - Backend row: label `t('dashboard:backendStatus.connected')` or `disconnected`, Badge component showing status
       - Uses `useBackendStatus()` hook
       - Connected: Badge variant="default" with green bg (bg-success text-success-foreground)
       - Disconnected: Badge variant="destructive"
       - Loading: Skeleton component (h-5 w-24)

    2. Create `src/features/dashboard/components/EmptyState.tsx`:
       - Props: `{ icon: LucideIcon, heading: string, body: string }`
       - Centered layout with icon (48px, muted-foreground), heading (18px semibold), body text (15px muted-foreground)
       - Spacious vertical padding (py-12)

    3. Create `src/features/dashboard/DashboardPage.tsx`:
       - Centered content: `max-w-[640px] mx-auto`
       - Hero section: Display heading (30px semibold) `t('dashboard:heading')`, Body subheading (15px muted-foreground) `t('dashboard:subheading')`
       - SystemStatus card
       - Recent simulations section: heading `t('dashboard:recentSimulations')`, then EmptyState with BarChart3 icon, heading `t('dashboard:emptyState.heading')`, body `t('dashboard:emptyState.body')`
       - Quick-start CTA: Button (size="lg", full-width on mobile `w-full sm:w-auto`) with text `t('dashboard:quickStart')`, accent/primary variant
       - Spacious layout: sections separated by 32px gaps

    4. Create `tests/components/layout.test.tsx` with basic smoke tests:
       - Test setup: Create a test utility wrapper that provides i18n + QueryClient + MemoryRouter
       - Test 1: "AppShell renders header, sidebar, and footer" -- render AppShell with MemoryRouter, assert header, sidebar content visible
       - Test 2: "Stepper renders 5 pipeline stages" -- render Stepper, query for all 5 Polish stage names
       - Test 3: "DashboardPage renders hero heading" -- render DashboardPage, assert "Witaj w Miroslaw Ryba" text present
       - Test 4: "LanguageSwitcher changes language" -- render switcher, click English option, verify text changes
       - Use `@testing-library/react` render + screen queries
       - Import i18n config before tests so translations are available

    IMPORTANT: All text via `t()` -- no hardcoded Polish or English strings in components.
    IMPORTANT: Dashboard uses `max-w-[640px]` per UI-SPEC (centered content for form-like views).
    IMPORTANT: Use Skeleton component from shadcn for loading states in SystemStatus.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/features/dashboard/DashboardPage.tsx` succeeds
    - `grep -q "useTranslation" src/features/dashboard/DashboardPage.tsx` succeeds
    - `grep -q "max-w-\[640px\]" src/features/dashboard/DashboardPage.tsx` succeeds (centered content)
    - `test -f src/features/dashboard/components/SystemStatus.tsx` succeeds
    - `grep -q "useBackendStatus" src/features/dashboard/components/SystemStatus.tsx` succeeds
    - `test -f src/features/dashboard/components/EmptyState.tsx` succeeds
    - `test -f tests/components/layout.test.tsx` succeeds
    - `pnpm build` exits 0
    - `pnpm test` exits 0
  </acceptance_criteria>
  <done>
    Dashboard page renders with hero heading, system status card (backend connection indicator), empty simulations state, and quick-start CTA button. Layout tests pass covering AppShell rendering, stepper stages, dashboard content, and language switching.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm test` passes with layout tests
- App shell has header with logo, breadcrumb, language switcher, config button, backend status dot
- Sidebar shows 5 pipeline stages with Polish names
- Dashboard shows hero, system status, empty state, CTA
- Footer shows version, GitHub, AGPL
- Language switcher toggles all text between Polish and English
- Sidebar is fixed on desktop, Sheet drawer on tablet
</verification>

<success_criteria>
- Complete app shell visible at `/` with all layout elements
- 5 pipeline stages visible in sidebar stepper (all locked for Phase 2)
- Language switcher changes all visible text between PL and EN
- Dashboard displays hero, system status, empty simulations, quick-start button
- All text sourced from i18n translation files (zero hardcoded strings)
- `pnpm build && pnpm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-cross-cutting-concerns/02-02-SUMMARY.md`
</output>
