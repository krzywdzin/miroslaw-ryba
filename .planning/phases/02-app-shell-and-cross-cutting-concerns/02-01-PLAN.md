---
phase: 02-app-shell-and-cross-cutting-concerns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
  - pnpm-lock.yaml
  - src/styles/globals.css
  - src/i18n/config.ts
  - src/i18n/index.ts
  - src/locales/pl/common.json
  - src/locales/pl/navigation.json
  - src/locales/pl/dashboard.json
  - src/locales/pl/errors.json
  - src/locales/en/common.json
  - src/locales/en/navigation.json
  - src/locales/en/dashboard.json
  - src/locales/en/errors.json
  - src/lib/utils.ts
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/breadcrumb.tsx
  - src/components/ui/collapsible.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/sonner.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - UIUX-01

must_haves:
  truths:
    - "i18next loads Polish as default language"
    - "All translation namespaces (common, navigation, dashboard, errors) load without errors"
    - "shadcn/ui components render without errors"
    - "Tailwind CSS v4 compiles and applies styles"
    - "Geist font is loaded and applied as default sans font"
  artifacts:
    - path: "src/i18n/config.ts"
      provides: "i18next initialization with Polish default, English fallback"
      exports: ["default (i18n instance)"]
    - path: "src/styles/globals.css"
      provides: "Tailwind v4 CSS-first theme with blue accent, Geist font, design tokens"
      contains: "@import \"tailwindcss\""
    - path: "src/locales/pl/common.json"
      provides: "Polish translations for shared UI strings"
    - path: "src/lib/utils.ts"
      provides: "cn() utility for Tailwind class merging"
      exports: ["cn"]
    - path: "components.json"
      provides: "shadcn/ui configuration for Tailwind v4 mode"
  key_links:
    - from: "src/i18n/config.ts"
      to: "src/locales/pl/*.json"
      via: "static imports of translation JSON files"
      pattern: "import.*from.*locales/pl"
    - from: "src/styles/globals.css"
      to: "Tailwind v4"
      via: "@import tailwindcss and @theme directive"
      pattern: "@import.*tailwindcss"
---

<objective>
Install and configure the design system (shadcn/ui + Tailwind CSS v4 + Geist font), i18n framework (react-i18next with Polish/English), and all shared dependencies that Plans 02 and 03 build upon.

Purpose: Establish the foundational tooling layer -- design tokens, component library, translation system -- so that layout and feature plans can use shadcn components and t() translations immediately without setup overhead.

Output: Configured shadcn/ui with 11 components, i18next with 4 namespaces in PL/EN, Tailwind v4 CSS theme with blue accent and Geist font, React Router installed.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-RESEARCH.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-UI-SPEC.md
@.planning/phases/02-app-shell-and-cross-cutting-concerns/02-CONTEXT.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From src/app/providers.tsx:
```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { ReactQueryDevtools } from '@tanstack/react-query-devtools'
import type { ReactNode } from 'react'

export function Providers({ children }: { children: ReactNode }) {
  // QueryClientProvider wrapping children
}
```

From src/app/main.tsx:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { Providers } from './providers'
import App from './App'
// Renders StrictMode > Providers > App
```

From vite.config.ts:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
// plugins: [react()], resolve.alias: { '@': '/src' }
```

From package.json:
```json
{
  "dependencies": {
    "@tanstack/react-query": "^5.91.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.0",
    "zustand": "^5.0.0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, init shadcn/ui, and configure Tailwind v4 theme</name>
  <files>
    package.json, pnpm-lock.yaml, vite.config.ts, src/styles/globals.css, src/lib/utils.ts, components.json,
    src/components/ui/button.tsx, src/components/ui/skeleton.tsx, src/components/ui/badge.tsx,
    src/components/ui/separator.tsx, src/components/ui/tooltip.tsx, src/components/ui/breadcrumb.tsx,
    src/components/ui/collapsible.tsx, src/components/ui/sheet.tsx, src/components/ui/dropdown-menu.tsx,
    src/components/ui/alert.tsx, src/components/ui/sonner.tsx
  </files>
  <read_first>
    package.json, vite.config.ts, vitest.config.ts, src/app/main.tsx
  </read_first>
  <action>
    1. Install all Phase 2 dependencies in one command:
       ```bash
       pnpm add react-router react-i18next i18next i18next-browser-languagedetector lucide-react sonner @fontsource-variable/geist
       pnpm add -D tailwindcss @tailwindcss/vite
       ```

    2. Add the Tailwind v4 Vite plugin to vite.config.ts:
       ```typescript
       import tailwindcss from '@tailwindcss/vite'
       // Add to plugins array: tailwindcss()
       // Keep existing react() plugin and proxy config
       ```

    3. Initialize shadcn/ui. Run `pnpm dlx shadcn@latest init` and configure for:
       - Style: Default
       - Base color: Neutral (we override with blue accent in CSS)
       - CSS variables: Yes
       - Tailwind CSS v4: Yes (CSS-first, NO tailwind.config.js)
       - Components alias: @/components
       - Utils alias: @/lib/utils
       - React Server Components: No

       If the interactive init is problematic, manually create `components.json`:
       ```json
       {
         "$schema": "https://ui.shadcn.com/schema.json",
         "style": "default",
         "rsc": false,
         "tsx": true,
         "tailwind": {
           "config": "",
           "css": "src/styles/globals.css",
           "baseColor": "neutral",
           "cssVariables": true
         },
         "aliases": {
           "components": "@/components",
           "utils": "@/lib/utils",
           "ui": "@/components/ui",
           "hooks": "@/hooks",
           "lib": "@/lib"
         },
         "iconLibrary": "lucide"
       }
       ```

    4. After shadcn init, customize `src/styles/globals.css` to match the project theme. Replace the generated CSS variables with these exact values from the UI-SPEC:
       ```css
       @import "tailwindcss";
       @import "@fontsource-variable/geist";

       :root {
         --radius: 0.5rem;
         --background: hsl(0 0% 100%);
         --foreground: hsl(240 10% 4%);
         --card: hsl(0 0% 100%);
         --card-foreground: hsl(240 10% 4%);
         --popover: hsl(0 0% 100%);
         --popover-foreground: hsl(240 10% 4%);
         --primary: hsl(217 91% 60%);
         --primary-foreground: hsl(0 0% 100%);
         --secondary: hsl(240 5% 96%);
         --secondary-foreground: hsl(240 6% 10%);
         --muted: hsl(240 5% 96%);
         --muted-foreground: hsl(240 4% 46%);
         --accent: hsl(217 91% 60%);
         --accent-foreground: hsl(0 0% 100%);
         --destructive: hsl(0 84% 60%);
         --destructive-foreground: hsl(0 0% 100%);
         --border: hsl(240 6% 90%);
         --input: hsl(240 6% 90%);
         --ring: hsl(217 91% 60%);
         --success: hsl(142 71% 45%);
         --success-foreground: hsl(0 0% 100%);
         --warning: hsl(38 92% 50%);
         --warning-foreground: hsl(0 0% 100%);
         --header-height: 56px;
         --footer-height: 40px;
         --sidebar-width: 280px;
         --sidebar-width-collapsed: 64px;
         --content-max-width: 960px;
       }

       @theme inline {
         --font-sans: "Geist Variable", ui-sans-serif, system-ui, sans-serif;
         --color-background: var(--background);
         --color-foreground: var(--foreground);
         --color-card: var(--card);
         --color-card-foreground: var(--card-foreground);
         --color-popover: var(--popover);
         --color-popover-foreground: var(--popover-foreground);
         --color-primary: var(--primary);
         --color-primary-foreground: var(--primary-foreground);
         --color-secondary: var(--secondary);
         --color-secondary-foreground: var(--secondary-foreground);
         --color-muted: var(--muted);
         --color-muted-foreground: var(--muted-foreground);
         --color-accent: var(--accent);
         --color-accent-foreground: var(--accent-foreground);
         --color-destructive: var(--destructive);
         --color-destructive-foreground: var(--destructive-foreground);
         --color-border: var(--border);
         --color-input: var(--input);
         --color-ring: var(--ring);
         --color-success: var(--success);
         --color-success-foreground: var(--success-foreground);
         --color-warning: var(--warning);
         --color-warning-foreground: var(--warning-foreground);
         --radius-sm: calc(var(--radius) - 4px);
         --radius-md: calc(var(--radius));
         --radius-lg: calc(var(--radius) + 4px);
         --radius-xl: calc(var(--radius) + 8px);
       }

       @layer base {
         * {
           @apply border-border;
         }
         body {
           @apply bg-background text-foreground font-sans;
           font-feature-settings: "rlig" 1, "calt" 1;
         }
       }
       ```

    5. Import globals.css in src/app/main.tsx (add `import '@/styles/globals.css'` before other imports).

    6. Install all 11 shadcn/ui components:
       ```bash
       pnpm dlx shadcn@latest add button skeleton badge separator tooltip breadcrumb collapsible sheet dropdown-menu alert sonner
       ```

    7. Verify `src/lib/utils.ts` exists with the `cn()` function (shadcn creates this). If not, create it:
       ```typescript
       import { clsx, type ClassValue } from 'clsx'
       import { twMerge } from 'tailwind-merge'

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```

    IMPORTANT: Do NOT create a tailwind.config.js file. Tailwind v4 uses CSS-first configuration. If shadcn init creates one, delete it.
    IMPORTANT: Verify the @tailwindcss/vite plugin is in vite.config.ts plugins array alongside react().
    IMPORTANT: Use `@fontsource-variable/geist` (not `@fontsource/geist`) for variable font weights.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "tailwindcss" vite.config.ts` succeeds (Tailwind plugin added)
    - `grep -q "@import" src/styles/globals.css` succeeds (Tailwind imported)
    - `grep -q "hsl(217 91% 60%)" src/styles/globals.css` succeeds (blue accent applied)
    - `grep -q "Geist Variable" src/styles/globals.css` succeeds (Geist font configured)
    - `ls src/components/ui/button.tsx src/components/ui/skeleton.tsx src/components/ui/sheet.tsx` succeeds (shadcn components installed)
    - `test -f components.json` succeeds (shadcn config exists)
    - `test ! -f tailwind.config.js` succeeds (no v3-style JS config)
    - `test -f src/lib/utils.ts` succeeds (cn utility exists)
    - `grep -q "globals.css" src/app/main.tsx` succeeds (CSS imported in entry)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    shadcn/ui initialized with Tailwind v4, 11 components installed, blue accent theme configured, Geist font loaded, project builds cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure i18n with Polish/English translations</name>
  <files>
    src/i18n/config.ts, src/i18n/index.ts,
    src/locales/pl/common.json, src/locales/pl/navigation.json, src/locales/pl/dashboard.json, src/locales/pl/errors.json,
    src/locales/en/common.json, src/locales/en/navigation.json, src/locales/en/dashboard.json, src/locales/en/errors.json
  </files>
  <read_first>
    src/api/errors.ts, src/app/providers.tsx
  </read_first>
  <action>
    1. Create `src/i18n/config.ts` with i18next initialization:
       - Use `i18next-browser-languagedetector` with detection order: `['localStorage', 'navigator']`, caches: `['localStorage']`, lookupLocalStorage: `'i18nextLng'`
       - Default language: `'pl'`, fallback: `'en'`
       - Namespaces: `['common', 'navigation', 'dashboard', 'errors']`, defaultNS: `'common'`
       - Interpolation: `escapeValue: false` (React handles escaping)
       - Import all translation JSON files statically (no lazy loading -- small app)
       - Chain: `i18n.use(LanguageDetector).use(initReactI18next).init({...})`
       - Export `i18n` as default

    2. Create `src/i18n/index.ts` re-exporting the config for clean imports:
       ```typescript
       export { default } from './config'
       ```

    3. Create Polish translation files with proper Unicode diacritics:

       `src/locales/pl/common.json`:
       ```json
       {
         "appTitle": "Miroslaw Ryba",
         "retry": "Spróbuj ponownie",
         "loading": "Ładowanie...",
         "connected": "Połączony",
         "disconnected": "Rozłączony",
         "settings": "Ustawienia",
         "language": "Język",
         "save": "Zapisz",
         "cancel": "Anuluj",
         "close": "Zamknij",
         "back": "Wstecz",
         "next": "Dalej",
         "simulation_one": "{{count}} symulacja",
         "simulation_few": "{{count}} symulacje",
         "simulation_many": "{{count}} symulacji",
         "simulation_other": "{{count}} symulacji",
         "agent_one": "{{count}} agent",
         "agent_few": "{{count}} agenty",
         "agent_many": "{{count}} agentów",
         "agent_other": "{{count}} agentów"
       }
       ```

       `src/locales/pl/navigation.json`:
       ```json
       {
         "dashboard": "Panel główny",
         "stage1": "Budowa grafu",
         "stage2": "Środowisko",
         "stage3": "Symulacja",
         "stage4": "Raport",
         "stage5": "Dialog",
         "settings": "Ustawienia",
         "breadcrumb": {
           "home": "Strona główna"
         },
         "status": {
           "completed": "Ukończony",
           "current": "Bieżący",
           "locked": "Zablokowany"
         }
       }
       ```

       `src/locales/pl/dashboard.json`:
       ```json
       {
         "heading": "Witaj w Miroslaw Ryba",
         "subheading": "System predykcji oparty na symulacji wieloagentowej",
         "quickStart": "Rozpocznij predykcję",
         "systemStatus": "Status systemu",
         "recentSimulations": "Ostatnie symulacje",
         "emptyState": {
           "heading": "Brak symulacji",
           "body": "Nie przeprowadzono jeszcze żadnej symulacji. Kliknij \"Rozpocznij predykcję\", aby zacząć."
         },
         "backendStatus": {
           "connected": "Backend połączony",
           "disconnected": "Backend rozłączony"
         }
       }
       ```

       `src/locales/pl/errors.json`:
       ```json
       {
         "networkError": "Błąd połączenia z serwerem. Ponowna próba...",
         "networkErrorFinal": "Nie udało się połączyć z serwerem.",
         "unknown": "Wystąpił nieoczekiwany błąd.",
         "timeout": "Żądanie przekroczyło limit czasu.",
         "notFound": "Nie znaleziono zasobu.",
         "serverError": "Wewnętrzny błąd serwera.",
         "invalidApiKey": "Nieprawidłowy klucz API.",
         "modelUnavailable": "Model jest niedostępny.",
         "projectNotExist": "Projekt nie istnieje.",
         "taskNotExist": "Zadanie nie istnieje.",
         "uploadFailed": "Przesyłanie pliku nie powiodło się.",
         "invalidParams": "Błąd parametrów.",
         "simulationNotExist": "Symulacja nie istnieje.",
         "reportNotExist": "Raport nie istnieje."
       }
       ```

    4. Create English translation files (same structure):

       `src/locales/en/common.json`:
       ```json
       {
         "appTitle": "Miroslaw Ryba",
         "retry": "Try again",
         "loading": "Loading...",
         "connected": "Connected",
         "disconnected": "Disconnected",
         "settings": "Settings",
         "language": "Language",
         "save": "Save",
         "cancel": "Cancel",
         "close": "Close",
         "back": "Back",
         "next": "Next",
         "simulation_one": "{{count}} simulation",
         "simulation_other": "{{count}} simulations",
         "agent_one": "{{count}} agent",
         "agent_other": "{{count}} agents"
       }
       ```

       `src/locales/en/navigation.json`:
       ```json
       {
         "dashboard": "Dashboard",
         "stage1": "Graph Construction",
         "stage2": "Environment",
         "stage3": "Simulation",
         "stage4": "Report",
         "stage5": "Dialogue",
         "settings": "Settings",
         "breadcrumb": {
           "home": "Home"
         },
         "status": {
           "completed": "Completed",
           "current": "Current",
           "locked": "Locked"
         }
       }
       ```

       `src/locales/en/dashboard.json`:
       ```json
       {
         "heading": "Welcome to Miroslaw Ryba",
         "subheading": "Multi-agent simulation-based prediction system",
         "quickStart": "Start prediction",
         "systemStatus": "System status",
         "recentSimulations": "Recent simulations",
         "emptyState": {
           "heading": "No simulations",
           "body": "No simulations yet. Click \"Start prediction\" to begin."
         },
         "backendStatus": {
           "connected": "Backend connected",
           "disconnected": "Backend disconnected"
         }
       }
       ```

       `src/locales/en/errors.json`:
       ```json
       {
         "networkError": "Server connection error. Retrying...",
         "networkErrorFinal": "Could not connect to server.",
         "unknown": "An unexpected error occurred.",
         "timeout": "Request timed out.",
         "notFound": "Resource not found.",
         "serverError": "Internal server error.",
         "invalidApiKey": "Invalid API key.",
         "modelUnavailable": "Model is unavailable.",
         "projectNotExist": "Project does not exist.",
         "taskNotExist": "Task does not exist.",
         "uploadFailed": "File upload failed.",
         "invalidParams": "Invalid parameters.",
         "simulationNotExist": "Simulation does not exist.",
         "reportNotExist": "Report does not exist."
       }
       ```

    5. Import i18n config in `src/app/main.tsx` BEFORE React rendering:
       Add `import '@/i18n/config'` as the first import after the globals.css import.

    IMPORTANT: Polish translation files MUST use proper Unicode diacritics (ą, ć, ę, ł, ń, ó, ś, ź, ż). The action text above shows the correct characters.
    IMPORTANT: English only needs `_one` and `_other` plural suffixes (2 forms). Polish needs `_one`, `_few`, `_many`, `_other` (4 forms).
    IMPORTANT: Translation keys must be flat within each namespace (max 2 levels deep for grouping like `emptyState.heading`).
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm build && node -e "const pl = require('./src/locales/pl/common.json'); const en = require('./src/locales/en/common.json'); if(!pl.retry || !en.retry) throw new Error('missing keys')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/i18n/config.ts` succeeds
    - `grep -q "lng: 'pl'" src/i18n/config.ts` succeeds (Polish default)
    - `grep -q "fallbackLng: 'en'" src/i18n/config.ts` or `grep -q "fallbackLng" src/i18n/config.ts` succeeds
    - `grep -q "LanguageDetector" src/i18n/config.ts` succeeds (browser detection configured)
    - `test -f src/locales/pl/common.json && test -f src/locales/pl/navigation.json && test -f src/locales/pl/dashboard.json && test -f src/locales/pl/errors.json` succeeds (all PL namespaces)
    - `test -f src/locales/en/common.json && test -f src/locales/en/navigation.json && test -f src/locales/en/dashboard.json && test -f src/locales/en/errors.json` succeeds (all EN namespaces)
    - `grep -q "simulation_few" src/locales/pl/common.json` succeeds (Polish plural forms present)
    - `grep -q "i18n" src/app/main.tsx` succeeds (i18n imported at entry)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    i18next configured with Polish default, English fallback, 4 namespaces (common, navigation, dashboard, errors), Polish 4-form plurals, browser language detection with localStorage persistence. All translation files created with proper Unicode diacritics.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes (TypeScript compilation + Vite build)
- shadcn/ui components exist in src/components/ui/ (at least button, skeleton, sheet, dropdown-menu)
- globals.css contains Tailwind v4 @import and blue accent theme
- i18n config loads Polish by default
- All 8 translation files exist (4 namespaces x 2 languages)
- No tailwind.config.js exists (Tailwind v4 CSS-first)
</verification>

<success_criteria>
- Project builds with `pnpm build` (TypeScript + Vite)
- shadcn/ui components are importable from @/components/ui/*
- i18next initializes with Polish as default language
- Translation files contain all UI copy from the UI-SPEC copywriting contract
- Tailwind v4 CSS theme applies blue accent color (hsl(217 91% 60%))
- Geist font is the default sans-serif via @fontsource-variable/geist
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-and-cross-cutting-concerns/02-01-SUMMARY.md`
</output>
