---
phase: 01-infrastructure-and-api-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/api/client.ts
  - src/api/errors.ts
  - src/api/schemas/graph.ts
  - src/api/schemas/simulation.ts
  - src/api/schemas/report.ts
  - src/api/schemas/common.ts
  - src/api/graph.ts
  - src/api/simulation.ts
  - src/api/report.ts
  - src/types/api.ts
  - src/hooks/usePolling.ts
  - tests/api/client.test.ts
  - tests/api/errors.test.ts
  - tests/api/schemas.test.ts
  - tests/hooks/usePolling.test.ts
  - openapi/mirofish-api.yaml
autonomous: true
requirements:
  - INFR-03
  - INFR-04

must_haves:
  truths:
    - "API client can make typed requests to all 3 backend blueprints (graph, simulation, report)"
    - "Zod schemas validate backend response shapes at runtime"
    - "Chinese error messages are intercepted and mapped to Polish/English equivalents"
    - "Polling hook starts fetching at interval and stops when isComplete returns true"
    - "All API types are inferred from Zod schemas (no duplicate type definitions)"
  artifacts:
    - path: "src/api/client.ts"
      provides: "Base fetch wrapper with error interception"
      exports: ["apiRequest", "ApiError"]
    - path: "src/api/errors.ts"
      provides: "Chinese-to-Polish/English error mapping"
      exports: ["mapChineseError", "ApiError"]
    - path: "src/api/schemas/graph.ts"
      provides: "Zod schemas for graph_bp endpoints"
      contains: "ProjectSchema"
    - path: "src/api/schemas/simulation.ts"
      provides: "Zod schemas for simulation_bp endpoints"
      contains: "SimulationSchema"
    - path: "src/api/schemas/report.ts"
      provides: "Zod schemas for report_bp endpoints"
      contains: "ReportSchema"
    - path: "src/api/graph.ts"
      provides: "Typed API functions for graph blueprint"
      exports: ["graphApi"]
    - path: "src/api/simulation.ts"
      provides: "Typed API functions for simulation blueprint"
      exports: ["simulationApi"]
    - path: "src/api/report.ts"
      provides: "Typed API functions for report blueprint"
      exports: ["reportApi"]
    - path: "src/hooks/usePolling.ts"
      provides: "Generic polling hook with TanStack Query"
      exports: ["usePollingQuery"]
    - path: "openapi/mirofish-api.yaml"
      provides: "OpenAPI 3.1 spec documenting all backend endpoints"
      contains: "/api/graph"
  key_links:
    - from: "src/api/graph.ts"
      to: "src/api/client.ts"
      via: "imports apiRequest for all HTTP calls"
      pattern: "import.*apiRequest.*client"
    - from: "src/api/graph.ts"
      to: "src/api/schemas/graph.ts"
      via: "passes Zod schemas to apiRequest for validation"
      pattern: "import.*Schema.*schemas/graph"
    - from: "src/api/client.ts"
      to: "src/api/errors.ts"
      via: "calls mapChineseError on error responses"
      pattern: "mapChineseError"
    - from: "src/hooks/usePolling.ts"
      to: "@tanstack/react-query"
      via: "uses useQuery with refetchInterval"
      pattern: "refetchInterval"
    - from: "src/types/api.ts"
      to: "src/api/schemas/*.ts"
      via: "z.infer extracts types from Zod schemas"
      pattern: "z\\.infer"
---

<objective>
Build the complete typed API client layer with Zod-validated schemas for all MiroFish backend endpoints, an error mapping layer for Chinese messages, and a polling hook for real-time updates.

Purpose: All feature phases (3-8) need typed, validated API calls. Building this layer now means feature work can focus on UI, not plumbing.
Output: Typed API modules for graph/simulation/report blueprints, polling hook, OpenAPI spec, unit tests.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-api-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-api-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-and-api-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - executor needs these paths and patterns -->

From src/app/providers.tsx:
```typescript
// QueryClient is already configured here - polling hook will use this context
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
```

From vite.config.ts:
```typescript
// Proxy already configured: /api -> http://localhost:${BACKEND_PORT || 5050}
// API client should use '/api' as BASE_URL (relative, goes through proxy)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client base, error mapping, and Zod schemas for all blueprints</name>
  <files>src/api/client.ts, src/api/errors.ts, src/api/schemas/common.ts, src/api/schemas/graph.ts, src/api/schemas/simulation.ts, src/api/schemas/report.ts, src/types/api.ts</files>
  <read_first>
    - /Users/antonio/Documents/miroslaw_ryba/.planning/phases/01-infrastructure-and-api-foundation/01-RESEARCH.md (Complete Backend API Inventory section -- lines 466-549, Pattern 3: Typed API Client, Pattern 5: Error Mapping Layer)
    - /Users/antonio/Documents/miroslaw_ryba/vite.config.ts (verify proxy config exists)
    - /Users/antonio/Documents/miroslaw_ryba/package.json (verify zod is installed)
  </read_first>
  <action>
1. Create `src/api/errors.ts`:
   - Export `class ApiError extends Error` with fields: `status: number`, `rawMessage?: string`
   - Export `function mapChineseError(message: string, locale?: 'pl' | 'en'): string`
   - Initialize `CHINESE_ERROR_MAP` as `Record<string, { pl: string; en: string }>` with these starter entries:
     ```
     'Internal Server Error': { pl: 'Wewnetrzny blad serwera', en: 'Internal server error' }
     '项目不存在': { pl: 'Projekt nie istnieje', en: 'Project does not exist' }
     '任务不存在': { pl: 'Zadanie nie istnieje', en: 'Task does not exist' }
     '文件上传失败': { pl: 'Przesylanie pliku nie powiodlo sie', en: 'File upload failed' }
     '参数错误': { pl: 'Blad parametrow', en: 'Invalid parameters' }
     '模拟不存在': { pl: 'Symulacja nie istnieje', en: 'Simulation does not exist' }
     '报告不存在': { pl: 'Raport nie istnieje', en: 'Report does not exist' }
     ```
   - Default locale is `'pl'`
   - If no mapping found, return the original message unchanged

2. Create `src/api/client.ts`:
   - `const BASE_URL = '/api'` (relative -- goes through Vite proxy)
   - Export `async function apiRequest<T>(path: string, options?: RequestInit & { schema?: z.ZodType<T> }): Promise<T>`
   - Build URL as `${BASE_URL}${path}`
   - Default headers: `{ 'Content-Type': 'application/json' }` BUT if `options.body instanceof FormData`, delete the Content-Type header (let browser set multipart boundary)
   - On non-ok response: parse JSON body, call `mapChineseError(error.message || response.statusText)`, throw `new ApiError(mapped, response.status, original)`
   - On ok response: parse JSON, if `options.schema` provided call `schema.parse(json)`, otherwise return `json as T`
   - Import `z` from `'zod/v4'` (NOT from `'zod'`)

3. Create `src/api/schemas/common.ts`:
   - Import `z` from `'zod/v4'`
   - Export `apiResponse` factory: `<T extends z.ZodType>(dataSchema: T) => z.object({ success: z.boolean(), data: dataSchema, message: z.string().optional() })`
   - Export `apiListResponse` factory: `<T extends z.ZodType>(itemSchema: T) => z.object({ success: z.boolean(), data: z.array(itemSchema), count: z.number().optional() })`
   - Export `apiMessageResponse`: `z.object({ success: z.boolean(), message: z.string() })`

4. Create `src/api/schemas/graph.ts` with Zod v4 schemas for all graph_bp endpoints:
   - `ProjectSchema`: `z.object({ project_id: z.string(), project_name: z.string().optional(), simulation_requirement: z.string().optional() })`
   - `ProjectResponseSchema`: `apiResponse(ProjectSchema)`
   - `ProjectListResponseSchema`: `z.object({ success: z.boolean(), data: z.array(ProjectSchema), count: z.number() })`
   - `OntologyResponseSchema`: `apiResponse(z.object({ project_id: z.string(), ontology: z.unknown() }))`
   - `BuildResponseSchema`: `apiResponse(z.object({ project_id: z.string(), task_id: z.string() }))`
   - `TaskSchema`: `z.object({ task_id: z.string(), status: z.string(), progress: z.number().optional(), error: z.string().optional() })`
   - `TaskResponseSchema`: `apiResponse(TaskSchema)`
   - `TaskListResponseSchema`: `z.object({ success: z.boolean(), data: z.array(TaskSchema), count: z.number() })`
   - `GraphNodeSchema`: `z.object({ id: z.string(), name: z.string(), type: z.string().optional() })`
   - `GraphEdgeSchema`: `z.object({ source: z.string(), target: z.string(), relation: z.string().optional() })`
   - `GraphDataSchema`: `apiResponse(z.object({ nodes: z.array(GraphNodeSchema), edges: z.array(GraphEdgeSchema) }))`

5. Create `src/api/schemas/simulation.ts` with Zod v4 schemas for key simulation_bp endpoints:
   - `SimulationStateSchema`: `z.object({ simulation_id: z.string(), project_id: z.string(), status: z.string(), graph_id: z.string().optional(), enable_twitter: z.boolean().optional(), enable_reddit: z.boolean().optional() })`
   - `SimulationResponseSchema`: `apiResponse(SimulationStateSchema)`
   - `SimulationListResponseSchema`: `z.object({ success: z.boolean(), data: z.array(SimulationStateSchema), count: z.number() })`
   - `EntitySchema`: `z.object({ uuid: z.string(), name: z.string(), type: z.string(), description: z.string().optional() })`
   - `EntitiesResponseSchema`: `apiResponse(z.object({ filtered_count: z.number(), entities: z.array(EntitySchema), entity_types: z.array(z.string()) }))`
   - `ProfileSchema`: `z.object({ agent_id: z.string(), name: z.string(), personality: z.string().optional(), stance: z.string().optional(), platform: z.string().optional() })`
   - `ProfilesResponseSchema`: `apiResponse(z.object({ platform: z.string(), count: z.number(), profiles: z.array(ProfileSchema) }))`
   - `PrepareStatusSchema`: `apiResponse(z.object({ task_id: z.string(), status: z.string(), progress: z.number().optional(), prepare_info: z.unknown().optional() }))`
   - `RunStatusSchema`: `apiResponse(z.object({ runner_status: z.string(), current_round: z.number().optional(), total_rounds: z.number().optional(), progress_percent: z.number().optional() }))`
   - `ActionSchema`: `z.object({ agent_id: z.string(), action_type: z.string(), content: z.string().optional(), round_num: z.number().optional(), platform: z.string().optional(), timestamp: z.string().optional() })`
   - `ActionsResponseSchema`: `z.object({ success: z.boolean(), data: z.object({ count: z.number(), actions: z.array(ActionSchema) }) })`
   - `TimelineResponseSchema`: `apiResponse(z.object({ rounds_count: z.number(), timeline: z.array(z.unknown()) }))`
   - `InterviewResponseSchema`: `apiResponse(z.object({ agent_id: z.string(), prompt: z.string(), result: z.unknown() }))`
   - `EnvStatusResponseSchema`: `apiResponse(z.object({ env_alive: z.boolean(), twitter_available: z.boolean().optional(), reddit_available: z.boolean().optional() }))`

6. Create `src/api/schemas/report.ts` with Zod v4 schemas for report_bp endpoints:
   - `ReportSchema`: `z.object({ report_id: z.string(), simulation_id: z.string().optional(), markdown_content: z.string().optional(), outline: z.unknown().optional(), status: z.string().optional() })`
   - `ReportResponseSchema`: `apiResponse(ReportSchema)`
   - `ReportListResponseSchema`: `z.object({ success: z.boolean(), data: z.array(ReportSchema), count: z.number() })`
   - `ReportGenerateResponseSchema`: `apiResponse(z.object({ simulation_id: z.string(), report_id: z.string(), task_id: z.string(), status: z.string() }))`
   - `ReportProgressSchema`: `apiResponse(z.object({ status: z.string(), progress: z.number().optional(), current_section: z.string().optional(), completed_sections: z.array(z.string()).optional() }))`
   - `ReportSectionsSchema`: `apiResponse(z.object({ sections: z.array(z.unknown()), total_sections: z.number(), is_complete: z.boolean() }))`
   - `ReportCheckSchema`: `apiResponse(z.object({ has_report: z.boolean(), report_status: z.string().optional(), interview_unlocked: z.boolean().optional() }))`
   - `ChatResponseSchema`: `apiResponse(z.object({ response: z.string(), tool_calls: z.array(z.unknown()).optional(), sources: z.array(z.unknown()).optional() }))`
   - `AgentLogSchema`: `apiResponse(z.object({ logs: z.array(z.unknown()), total_lines: z.number(), has_more: z.boolean() }))`

7. Create `src/types/api.ts`:
   - Use `z.infer<typeof Schema>` to extract TypeScript types from every schema
   - Export all types: `Project`, `Task`, `GraphData`, `GraphNode`, `GraphEdge`, `SimulationState`, `Entity`, `Profile`, `Action`, `Report`, etc.
   - Example: `export type Project = z.infer<typeof ProjectSchema>`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm typecheck && echo "TYPECHECK PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/client.ts` contains `const BASE_URL = '/api'`
    - `src/api/client.ts` contains `export async function apiRequest`
    - `src/api/client.ts` contains `mapChineseError`
    - `src/api/client.ts` contains `from 'zod/v4'`
    - `src/api/client.ts` contains `instanceof FormData` check for Content-Type header handling
    - `src/api/errors.ts` contains `export class ApiError extends Error`
    - `src/api/errors.ts` contains `export function mapChineseError`
    - `src/api/errors.ts` contains at least 5 entries in `CHINESE_ERROR_MAP`
    - `src/api/schemas/common.ts` contains `export const apiResponse` and `export const apiListResponse`
    - `src/api/schemas/graph.ts` contains `ProjectSchema` and `GraphDataSchema` and `TaskSchema`
    - `src/api/schemas/graph.ts` contains `from 'zod/v4'`
    - `src/api/schemas/simulation.ts` contains `SimulationStateSchema` and `EntitySchema` and `ProfileSchema` and `RunStatusSchema`
    - `src/api/schemas/report.ts` contains `ReportSchema` and `ChatResponseSchema` and `ReportProgressSchema`
    - `src/types/api.ts` contains `z.infer` and `export type Project`
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
  <done>Base API client with error mapping exists. Zod v4 schemas cover all 3 blueprints (graph, simulation, report). TypeScript types inferred from schemas. All source compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Typed API modules for graph, simulation, and report blueprints</name>
  <files>src/api/graph.ts, src/api/simulation.ts, src/api/report.ts</files>
  <read_first>
    - /Users/antonio/Documents/miroslaw_ryba/src/api/client.ts (apiRequest signature)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/graph.ts (available schemas)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/simulation.ts (available schemas)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/report.ts (available schemas)
    - /Users/antonio/Documents/miroslaw_ryba/.planning/phases/01-infrastructure-and-api-foundation/01-RESEARCH.md (Complete Backend API Inventory, lines 466-549)
  </read_first>
  <action>
1. Create `src/api/graph.ts` - export `const graphApi` object with methods for ALL graph_bp endpoints:
   - `getProject(projectId: string)` -> GET `/graph/project/${projectId}` with `ProjectResponseSchema`
   - `listProjects(limit = 50)` -> GET `/graph/project/list?limit=${limit}` with `ProjectListResponseSchema`
   - `deleteProject(projectId: string)` -> DELETE `/graph/project/${projectId}` with `apiMessageResponse`
   - `resetProject(projectId: string)` -> POST `/graph/project/${projectId}/reset` with `ProjectResponseSchema`
   - `generateOntology(formData: FormData)` -> POST `/graph/ontology/generate` with body `formData` (no Content-Type header -- FormData), with `OntologyResponseSchema`
   - `buildGraph(projectId: string)` -> POST `/graph/build` with body `{ project_id: projectId }`, with `BuildResponseSchema`
   - `getTask(taskId: string)` -> GET `/graph/task/${taskId}` with `TaskResponseSchema`
   - `listTasks()` -> GET `/graph/tasks` with `TaskListResponseSchema`
   - `getGraphData(graphId: string)` -> GET `/graph/data/${graphId}` with `GraphDataSchema`
   - `deleteGraph(graphId: string)` -> DELETE `/graph/delete/${graphId}` with `apiMessageResponse`

2. Create `src/api/simulation.ts` - export `const simulationApi` object with methods for ALL simulation_bp endpoints:
   - `getEntities(graphId: string, params?: { entity_types?: string; enrich?: boolean })` -> GET `/simulation/entities/${graphId}?...` with `EntitiesResponseSchema`
   - `getEntity(graphId: string, uuid: string)` -> GET `/simulation/entities/${graphId}/${uuid}`
   - `getEntitiesByType(graphId: string, type: string, enrich?: boolean)` -> GET `/simulation/entities/${graphId}/by-type/${type}?enrich=${enrich}`
   - `create(params: { project_id: string; graph_id?: string; enable_twitter?: boolean; enable_reddit?: boolean })` -> POST `/simulation/create` with `SimulationResponseSchema`
   - `prepare(params: { simulation_id: string; entity_types: string[]; use_llm_for_profiles?: boolean; parallel_profile_count?: number; force_regenerate?: boolean })` -> POST `/simulation/prepare` with `PrepareStatusSchema`
   - `prepareStatus(params: { task_id?: string; simulation_id?: string })` -> POST `/simulation/prepare/status` (NOTE: POST not GET) with `PrepareStatusSchema`
   - `get(simulationId: string)` -> GET `/simulation/${simulationId}` with `SimulationResponseSchema`
   - `list(projectId?: string)` -> GET `/simulation/list?project_id=${projectId}` with `SimulationListResponseSchema`
   - `history(limit = 20)` -> GET `/simulation/history?limit=${limit}`
   - `getProfiles(simId: string, platform?: string)` -> GET `/simulation/${simId}/profiles?platform=${platform}` with `ProfilesResponseSchema`
   - `getRealtimeProfiles(simId: string, platform?: string)` -> GET `/simulation/${simId}/profiles/realtime?platform=${platform}`
   - `getRealtimeConfig(simId: string)` -> GET `/simulation/${simId}/config/realtime`
   - `getConfig(simId: string)` -> GET `/simulation/${simId}/config`
   - `downloadConfig(simId: string)` -> GET `/simulation/${simId}/config/download` (returns blob)
   - `downloadScript(name: string)` -> GET `/simulation/script/${name}/download` (returns blob)
   - `generateProfiles(params: { graph_id: string; entity_types?: string[]; use_llm?: boolean; platform?: string })` -> POST `/simulation/generate-profiles` with `ProfilesResponseSchema`
   - `start(params: { simulation_id: string; platform?: string; max_rounds?: number; enable_graph_memory_update?: boolean; force?: boolean })` -> POST `/simulation/start`
   - `stop(params: { simulation_id: string })` -> POST `/simulation/stop`
   - `getRunStatus(simId: string)` -> GET `/simulation/${simId}/run-status` with `RunStatusSchema`
   - `getRunStatusDetail(simId: string, platform?: string)` -> GET `/simulation/${simId}/run-status/detail?platform=${platform}`
   - `getActions(simId: string, params?: { limit?: number; offset?: number; platform?: string; agent_id?: string; round_num?: number })` -> GET `/simulation/${simId}/actions?...` with `ActionsResponseSchema`
   - `getTimeline(simId: string, params?: { start_round?: number; end_round?: number })` -> GET `/simulation/${simId}/timeline?...` with `TimelineResponseSchema`
   - `getAgentStats(simId: string)` -> GET `/simulation/${simId}/agent-stats`
   - `getPosts(simId: string, params?: { platform?: string; limit?: number; offset?: number })` -> GET `/simulation/${simId}/posts?...`
   - `getComments(simId: string, params?: { post_id?: string; limit?: number; offset?: number })` -> GET `/simulation/${simId}/comments?...`
   - `interview(params: { simulation_id: string; agent_id: string; prompt: string; platform?: string; timeout?: number })` -> POST `/simulation/interview` with `InterviewResponseSchema`
   - `interviewBatch(params: { simulation_id: string; interviews: unknown[]; platform?: string; timeout?: number })` -> POST `/simulation/interview/batch`
   - `interviewAll(params: { simulation_id: string; prompt: string; platform?: string; timeout?: number })` -> POST `/simulation/interview/all`
   - `interviewHistory(params: { simulation_id: string; platform?: string; agent_id?: string; limit?: number })` -> POST `/simulation/interview/history`
   - `envStatus(params: { simulation_id: string })` -> POST `/simulation/env-status` with `EnvStatusResponseSchema`
   - `closeEnv(params: { simulation_id: string; timeout?: number })` -> POST `/simulation/close-env`

3. Create `src/api/report.ts` - export `const reportApi` object with methods for ALL report_bp endpoints:
   - `generate(params: { simulation_id: string; force_regenerate?: boolean })` -> POST `/report/generate` with `ReportGenerateResponseSchema`
   - `generateStatus(params: { task_id?: string; simulation_id?: string })` -> POST `/report/generate/status`
   - `get(reportId: string)` -> GET `/report/${reportId}` with `ReportResponseSchema`
   - `getBySimulation(simId: string)` -> GET `/report/by-simulation/${simId}`
   - `list(params?: { simulation_id?: string; limit?: number })` -> GET `/report/list?...` with `ReportListResponseSchema`
   - `download(reportId: string)` -> GET `/report/${reportId}/download` (returns blob)
   - `delete(reportId: string)` -> DELETE `/report/${reportId}` with `apiMessageResponse`
   - `chat(params: { simulation_id: string; message: string; chat_history?: unknown[] })` -> POST `/report/chat` with `ChatResponseSchema`
   - `getProgress(reportId: string)` -> GET `/report/${reportId}/progress` with `ReportProgressSchema`
   - `getSections(reportId: string)` -> GET `/report/${reportId}/sections` with `ReportSectionsSchema`
   - `getSection(reportId: string, index: number)` -> GET `/report/${reportId}/section/${index}`
   - `check(simId: string)` -> GET `/report/check/${simId}` with `ReportCheckSchema`
   - `getAgentLog(reportId: string, fromLine = 0)` -> GET `/report/${reportId}/agent-log?from_line=${fromLine}` with `AgentLogSchema`
   - `streamAgentLog(reportId: string)` -> GET `/report/${reportId}/agent-log/stream`
   - `getConsoleLog(reportId: string, fromLine = 0)` -> GET `/report/${reportId}/console-log?from_line=${fromLine}`
   - `streamConsoleLog(reportId: string)` -> GET `/report/${reportId}/console-log/stream`
   - `searchGraph(params: { graph_id: string; query: string; limit?: number })` -> POST `/report/tools/search`
   - `graphStatistics(params: { graph_id: string })` -> POST `/report/tools/statistics`

All methods use `apiRequest` from `./client`. For query parameters, build URLSearchParams and append to path. For POST bodies, use `JSON.stringify()`. For FormData uploads, pass FormData directly.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm typecheck && echo "TYPECHECK PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/graph.ts` contains `export const graphApi` with at least 10 methods
    - `src/api/graph.ts` contains `import { apiRequest }` from `./client`
    - `src/api/graph.ts` contains `generateOntology` with `FormData` parameter
    - `src/api/simulation.ts` contains `export const simulationApi` with at least 25 methods
    - `src/api/simulation.ts` contains `prepareStatus` using `method: 'POST'` (not GET)
    - `src/api/simulation.ts` contains `interview` and `interviewBatch` and `interviewAll`
    - `src/api/report.ts` contains `export const reportApi` with at least 15 methods
    - `src/api/report.ts` contains `chat` method with `ChatResponseSchema`
    - `src/api/report.ts` contains `generate` and `generateStatus`
    - All three files import schemas from `./schemas/*`
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
  <done>Three typed API modules (graphApi, simulationApi, reportApi) cover every endpoint in the MiroFish backend API inventory. Each method uses apiRequest with appropriate Zod schema validation. TypeScript compiles without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Polling hook + unit tests for API layer</name>
  <files>src/hooks/usePolling.ts, tests/api/errors.test.ts, tests/api/schemas.test.ts, tests/hooks/usePolling.test.ts, openapi/mirofish-api.yaml</files>
  <read_first>
    - /Users/antonio/Documents/miroslaw_ryba/src/api/errors.ts (mapChineseError signature, CHINESE_ERROR_MAP entries)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/graph.ts (schema exports to test)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/simulation.ts (schema exports)
    - /Users/antonio/Documents/miroslaw_ryba/src/api/schemas/report.ts (schema exports)
    - /Users/antonio/Documents/miroslaw_ryba/.planning/phases/01-infrastructure-and-api-foundation/01-RESEARCH.md (Pattern 4: Polling with TanStack Query)
    - /Users/antonio/Documents/miroslaw_ryba/src/app/providers.tsx (QueryClient configuration)
  </read_first>
  <behavior>
    - errors.test.ts: mapChineseError('项目不存在') returns 'Projekt nie istnieje' (default pl)
    - errors.test.ts: mapChineseError('项目不存在', 'en') returns 'Project does not exist'
    - errors.test.ts: mapChineseError('unknown message') returns 'unknown message' unchanged
    - errors.test.ts: ApiError has status and rawMessage fields
    - schemas.test.ts: ProjectSchema.parse({ project_id: '123' }) succeeds
    - schemas.test.ts: ProjectSchema.parse({}) throws (missing project_id)
    - schemas.test.ts: SimulationStateSchema.parse({ simulation_id: 'x', project_id: 'y', status: 'created' }) succeeds
    - schemas.test.ts: ReportSchema.parse({ report_id: 'r1' }) succeeds
    - usePolling.test.ts: hook calls queryFn at interval when enabled
    - usePolling.test.ts: hook stops polling when isComplete returns true
  </behavior>
  <action>
1. Create `src/hooks/usePolling.ts`:
   ```typescript
   import { useQuery } from '@tanstack/react-query'

   export function usePollingQuery<T>(
     queryKey: readonly unknown[],
     queryFn: () => Promise<T>,
     options: {
       isComplete: (data: T) => boolean
       interval?: number
       enabled?: boolean
     },
   ) {
     return useQuery({
       queryKey,
       queryFn,
       enabled: options.enabled ?? true,
       refetchInterval: (query) => {
         if (!query.state.data) return options.interval ?? 1000
         if (options.isComplete(query.state.data)) return false
         return options.interval ?? 1000
       },
       structuralSharing: true,
       refetchIntervalInBackground: false,
     })
   }
   ```

2. Create `tests/api/errors.test.ts` (RED first, then verify):
   - Test `mapChineseError` with known Chinese string '项目不存在' returns Polish 'Projekt nie istnieje'
   - Test `mapChineseError` with locale 'en' returns English 'Project does not exist'
   - Test `mapChineseError` with unknown string returns the original string unchanged
   - Test `ApiError` constructor sets `status`, `message`, and `rawMessage` correctly

3. Create `tests/api/schemas.test.ts` (RED first, then verify):
   - Test `ProjectSchema.parse({ project_id: '123' })` succeeds and returns correct shape
   - Test `ProjectSchema.parse({})` throws ZodError (missing required field)
   - Test `SimulationStateSchema.parse({ simulation_id: 'sim1', project_id: 'proj1', status: 'created' })` succeeds
   - Test `ReportSchema.parse({ report_id: 'rpt1' })` succeeds
   - Test `GraphNodeSchema.parse({ id: 'n1', name: 'Node 1' })` succeeds
   - Test `EntitySchema.parse({ uuid: 'e1', name: 'Entity', type: 'person' })` succeeds

4. Create `tests/hooks/usePolling.test.ts`:
   - Use `@testing-library/react` `renderHook` with a `QueryClientProvider` wrapper
   - Test that the hook returns `isLoading: true` initially
   - Test that when `isComplete` returns true, refetching stops (verify via mock call count)
   - Test that when `enabled: false`, queryFn is not called

5. Create `openapi/mirofish-api.yaml` -- OpenAPI 3.1 spec documenting all discovered endpoints:
   - info.title: "MiroFish Backend API"
   - info.version: "1.0.0"
   - info.description: "REST API for MiroFish AI prediction pipeline. Discovered from Flask source code."
   - servers: [{ url: "http://localhost:5050", description: "Local Docker backend" }]
   - Tags: graph, simulation, report
   - Include at minimum the 10 graph endpoints, 5 key simulation endpoints (create, prepare, start, stop, get), and 5 key report endpoints (generate, get, list, chat, check) with request/response schemas
   - This is a documentation artifact per user decision -- does not need to be exhaustive on day 1, but must cover the primary workflow endpoints

6. Run all tests: `pnpm test`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && pnpm test && pnpm typecheck && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/usePolling.ts` contains `export function usePollingQuery`
    - `src/hooks/usePolling.ts` contains `refetchInterval` and `refetchIntervalInBackground: false`
    - `src/hooks/usePolling.ts` contains `structuralSharing: true`
    - `tests/api/errors.test.ts` contains `mapChineseError` and at least 3 test cases
    - `tests/api/schemas.test.ts` contains `ProjectSchema.parse` and `SimulationStateSchema.parse`
    - `tests/hooks/usePolling.test.ts` contains `renderHook` and `QueryClientProvider`
    - `openapi/mirofish-api.yaml` contains `openapi: "3.1` and `MiroFish`
    - `openapi/mirofish-api.yaml` contains `/api/graph` and `/api/simulation` and `/api/report`
    - `pnpm test` exits with code 0 (all tests pass)
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
  <done>Polling hook implemented with TanStack Query. Error mapping, Zod schema validation, and polling hook all have passing unit tests. OpenAPI 3.1 spec documents the backend API surface. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes -- all API modules, schemas, and types compile
2. `pnpm test` passes -- error mapping, schema validation, and polling hook tests green
3. `pnpm lint` passes -- all new files pass ESLint
4. `src/api/graph.ts` exports `graphApi` with methods covering 10 graph_bp endpoints
5. `src/api/simulation.ts` exports `simulationApi` with methods covering 30+ simulation_bp endpoints
6. `src/api/report.ts` exports `reportApi` with methods covering 18 report_bp endpoints
7. `src/hooks/usePolling.ts` exports `usePollingQuery` with completion-based interval control
8. `openapi/mirofish-api.yaml` exists with endpoint documentation
</verification>

<success_criteria>
- Every MiroFish backend endpoint has a corresponding typed API function
- Zod schemas validate response shapes at runtime for all critical endpoints
- Chinese error messages are intercepted and mapped to Polish (default) or English
- Polling hook supports real-time updates with automatic stop on completion
- All new code has unit test coverage
- OpenAPI spec documents the API surface
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-api-foundation/01-02-SUMMARY.md`
</output>
