---
phase: 06-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/simulation/hooks/useSimulationStore.ts
  - src/features/simulation/hooks/useRunStatus.ts
  - src/features/simulation/hooks/useSimulationActions.ts
  - src/features/simulation/hooks/useSimulationTimeline.ts
  - src/features/simulation/hooks/useElapsedTime.ts
  - src/features/simulation/hooks/useScrollAnchor.ts
  - src/locales/pl/simulation.json
  - src/locales/en/simulation.json
  - src/i18n/config.ts
  - tests/features/simulation/simulation-store.test.ts
  - tests/features/simulation/run-status.test.ts
  - tests/features/simulation/elapsed-time.test.ts
autonomous: true
requirements:
  - SIMU-01
  - SIMU-03
  - SIMU-04

must_haves:
  truths:
    - "Simulation store tracks step, simulationId, activeRoundFilter, highlightedEventId"
    - "Run status hook polls /simulation/:id/run-status at 2000ms and auto-stops on completed/failed/stopped"
    - "Actions hook polls /simulation/:id/actions at 2500ms with platform filter param"
    - "Timeline hook polls /simulation/:id/timeline at 3000ms"
    - "Elapsed time hook counts seconds from a start timestamp while isRunning=true"
    - "Scroll anchor hook detects when scrollTop >= 50px and provides scrollToTop"
    - "Polish and English simulation translations are registered in i18n config"
  artifacts:
    - path: "src/features/simulation/hooks/useSimulationStore.ts"
      provides: "Zustand store with persist for simulation UI state"
      exports: ["useSimulationStore", "SimulationStep", "SimulationState"]
    - path: "src/features/simulation/hooks/useRunStatus.ts"
      provides: "Polling hook for run status"
      exports: ["useRunStatus"]
    - path: "src/features/simulation/hooks/useSimulationActions.ts"
      provides: "Polling hook for actions with platform filter"
      exports: ["useSimulationActions"]
    - path: "src/features/simulation/hooks/useSimulationTimeline.ts"
      provides: "Polling hook for timeline"
      exports: ["useSimulationTimeline"]
    - path: "src/features/simulation/hooks/useElapsedTime.ts"
      provides: "Elapsed time counter hook"
      exports: ["useElapsedTime"]
    - path: "src/features/simulation/hooks/useScrollAnchor.ts"
      provides: "Scroll position detection hook"
      exports: ["useScrollAnchor"]
    - path: "src/locales/pl/simulation.json"
      provides: "Polish simulation translations with plural forms"
    - path: "src/locales/en/simulation.json"
      provides: "English simulation translations"
  key_links:
    - from: "src/features/simulation/hooks/useRunStatus.ts"
      to: "src/hooks/usePolling.ts"
      via: "usePollingQuery wrapper"
      pattern: "usePollingQuery.*simulation.*run-status"
    - from: "src/features/simulation/hooks/useSimulationStore.ts"
      to: "zustand/middleware persist"
      via: "Zustand persist middleware"
      pattern: "persist.*mirofish-simulation"
---

<objective>
Create the simulation data layer: Zustand store for simulation UI state, polling hooks for run status / actions / timeline, utility hooks for elapsed time and scroll anchoring, and i18n translations for Polish/English.

Purpose: Provides the complete data and state foundation that all simulation UI components in Plan 02 will consume. Separating data from UI keeps both plans under context budget.
Output: 6 hooks, 2 translation files, i18n config update, 3 test files
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-simulation/06-CONTEXT.md
@.planning/phases/06-simulation/06-RESEARCH.md
@.planning/phases/06-simulation/06-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/hooks/usePolling.ts:
```typescript
export function usePollingQuery<T>(
  queryKey: readonly unknown[],
  queryFn: () => Promise<T>,
  options: {
    isComplete: (data: T) => boolean
    interval?: number
    enabled?: boolean
  },
)
```

From src/api/simulation.ts:
```typescript
export const simulationApi = {
  getRunStatus(simId: string) // -> RunStatusSchema
  getActions(simId: string, params?: { limit?: number; offset?: number; platform?: string; agent_id?: string; round_num?: number }) // -> ActionsResponseSchema
  getTimeline(simId: string, params?: { start_round?: number; end_round?: number }) // -> TimelineResponseSchema
  start(params: { simulation_id: string; platform?: string; max_rounds?: number; ... })
  stop(params: { simulation_id: string })
}
```

From src/api/schemas/simulation.ts:
```typescript
export const RunStatusSchema = apiResponse(z.object({
  runner_status: z.string(),
  current_round: z.number().optional(),
  total_rounds: z.number().optional(),
  progress_percent: z.number().optional(),
}))

export const ActionSchema = z.object({
  agent_id: z.string(),
  action_type: z.string(),
  content: z.string().optional(),
  round_num: z.number().optional(),
  platform: z.string().optional(),
  timestamp: z.string().optional(),
})

export const ActionsResponseSchema = z.object({
  success: z.boolean(),
  data: z.object({
    count: z.number(),
    actions: z.array(ActionSchema),
  }),
})

export const TimelineResponseSchema = apiResponse(z.object({
  rounds_count: z.number(),
  timeline: z.array(z.unknown()),
}))
```

From src/features/environment/hooks/useEnvironmentStore.ts (pattern reference):
```typescript
export const useEnvironmentStore = create<EnvironmentState>()(
  persist(
    (set) => ({ ...initialState, ...actions }),
    { name: 'mirofish-environment', partialize: (state) => ({ ...stateOnly }) },
  ),
)
```

From src/features/graph/hooks/useBuildStatus.ts (pattern reference):
```typescript
export function useBuildStatus(taskId: string | null) {
  return usePollingQuery(
    ['graph', 'task', taskId] as const,
    () => graphApi.getTask(taskId!),
    { enabled: !!taskId, interval: 2000, isComplete: (data) => status === 'completed' || status === 'failed' },
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create simulation store and polling hooks</name>
  <files>
    src/features/simulation/hooks/useSimulationStore.ts,
    src/features/simulation/hooks/useRunStatus.ts,
    src/features/simulation/hooks/useSimulationActions.ts,
    src/features/simulation/hooks/useSimulationTimeline.ts,
    src/features/simulation/hooks/useElapsedTime.ts,
    src/features/simulation/hooks/useScrollAnchor.ts,
    tests/features/simulation/simulation-store.test.ts,
    tests/features/simulation/run-status.test.ts,
    tests/features/simulation/elapsed-time.test.ts
  </files>
  <read_first>
    src/features/environment/hooks/useEnvironmentStore.ts,
    src/features/graph/hooks/useBuildStatus.ts,
    src/hooks/usePolling.ts,
    src/api/simulation.ts,
    src/api/schemas/simulation.ts,
    tests/features/environment/environment-store.test.ts
  </read_first>
  <behavior>
    - useSimulationStore initializes with step='idle', simulationId=null, activeRoundFilter=null, highlightedEventId=null, startTime=null
    - useSimulationStore.setStep('running') updates step to 'running'
    - useSimulationStore.setSimulationId('sim-1') stores the ID
    - useSimulationStore.setActiveRoundFilter(3) sets round filter to 3
    - useSimulationStore.setHighlightedEventId('evt-1') sets highlighted event
    - useSimulationStore.setStartTime(Date.now()) stores start timestamp
    - useSimulationStore.reset() returns all fields to defaults
    - useSimulationStore persist key is 'mirofish-simulation'
    - useSimulationStore excludes action functions from persisted state
    - useRunStatus returns polling query result with 2000ms interval
    - useRunStatus isComplete returns true for runner_status 'completed', 'failed', or 'stopped'
    - useRunStatus disabled when simulationId is null
    - useElapsedTime returns 0 when startTime is null
    - useElapsedTime increments every second when isRunning=true
    - useElapsedTime stops incrementing when isRunning=false
  </behavior>
  <action>
    Create `src/features/simulation/hooks/useSimulationStore.ts`:
    - Zustand store with persist middleware, following useEnvironmentStore pattern exactly
    - Types: `SimulationStep = 'idle' | 'running' | 'completed' | 'failed' | 'stopped'`
    - State fields: step (SimulationStep), simulationId (string|null), activeRoundFilter (number|null), highlightedEventId (string|null), startTime (number|null)
    - Actions: setStep, setSimulationId, setActiveRoundFilter, setHighlightedEventId, setStartTime, reset
    - Persist key: 'mirofish-simulation'
    - partialize: exclude action functions, only persist step, simulationId, startTime

    Create `src/features/simulation/hooks/useRunStatus.ts`:
    - Import `simulationApi` from '@/api/simulation' and `usePollingQuery` from '@/hooks/usePolling'
    - Accepts `simulationId: string | null`
    - Query key: `['simulation', 'run-status', simulationId] as const`
    - Calls `simulationApi.getRunStatus(simulationId!)`
    - enabled: `!!simulationId`
    - interval: 2000
    - isComplete: checks `data.data?.runner_status` for 'completed', 'failed', 'stopped'

    Create `src/features/simulation/hooks/useSimulationActions.ts`:
    - Accepts `simulationId: string | null, platform?: string`
    - Query key: `['simulation', 'actions', simulationId, platform] as const`
    - Calls `simulationApi.getActions(simulationId!, { platform, limit: 50 })`
    - enabled: `!!simulationId`
    - interval: 2500
    - isComplete: always returns false (tied to run status, not auto-stop)

    Create `src/features/simulation/hooks/useSimulationTimeline.ts`:
    - Accepts `simulationId: string | null`
    - Query key: `['simulation', 'timeline', simulationId] as const`
    - Calls `simulationApi.getTimeline(simulationId!)`
    - enabled: `!!simulationId`
    - interval: 3000
    - isComplete: always returns false

    Create `src/features/simulation/hooks/useElapsedTime.ts`:
    - Accepts `startTime: number | null, isRunning: boolean`
    - useState for elapsed (number, init 0)
    - useEffect: if !startTime or !isRunning, return
    - setInterval at 1000ms, tick = Math.floor((Date.now() - startTime) / 1000)
    - Cleanup clears interval
    - Returns elapsed (number of seconds)

    Create `src/features/simulation/hooks/useScrollAnchor.ts`:
    - Accepts `scrollRef: React.RefObject<HTMLDivElement | null>`
    - useState for isAtTop (boolean, init true)
    - useEffect: attach scroll listener on scrollRef.current, passive: true
    - Handler: setIsAtTop(el.scrollTop < 50)
    - scrollToTop callback: scrollRef.current?.scrollTo({ top: 0, behavior: 'smooth' })
    - Returns { isAtTop, scrollToTop }

    Write tests following patterns from tests/features/environment/environment-store.test.ts:
    - `tests/features/simulation/simulation-store.test.ts`: Test all state transitions, reset, persist key, partialize
    - `tests/features/simulation/run-status.test.ts`: Test that the hook calls usePollingQuery with correct args (mock usePollingQuery)
    - `tests/features/simulation/elapsed-time.test.ts`: Test elapsed counting with vi.useFakeTimers
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && npx vitest run tests/features/simulation/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/simulation/hooks/useSimulationStore.ts contains `export type SimulationStep = 'idle' | 'running' | 'completed' | 'failed' | 'stopped'`
    - src/features/simulation/hooks/useSimulationStore.ts contains `name: 'mirofish-simulation'`
    - src/features/simulation/hooks/useSimulationStore.ts contains `export const useSimulationStore`
    - src/features/simulation/hooks/useRunStatus.ts contains `interval: 2000`
    - src/features/simulation/hooks/useRunStatus.ts contains `simulationApi.getRunStatus`
    - src/features/simulation/hooks/useSimulationActions.ts contains `interval: 2500`
    - src/features/simulation/hooks/useSimulationActions.ts contains `platform`
    - src/features/simulation/hooks/useSimulationTimeline.ts contains `interval: 3000`
    - src/features/simulation/hooks/useElapsedTime.ts contains `setInterval`
    - src/features/simulation/hooks/useScrollAnchor.ts contains `scrollTop < 50`
    - tests/features/simulation/simulation-store.test.ts exits 0
    - tests/features/simulation/run-status.test.ts exits 0
    - tests/features/simulation/elapsed-time.test.ts exits 0
  </acceptance_criteria>
  <done>All 6 hooks exist with correct exports. Store persists under 'mirofish-simulation'. Polling hooks use correct intervals (2000/2500/3000ms). All 3 test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create i18n simulation namespace and register in config</name>
  <files>
    src/locales/pl/simulation.json,
    src/locales/en/simulation.json,
    src/i18n/config.ts
  </files>
  <read_first>
    src/i18n/config.ts,
    src/locales/pl/environment.json,
    src/locales/en/environment.json,
    .planning/phases/06-simulation/06-UI-SPEC.md
  </read_first>
  <action>
    Create `src/locales/pl/simulation.json` with ALL copy from the UI-SPEC Copywriting Contract:
    ```json
    {
      "progress": {
        "round": "Runda {{current}}/{{total}}",
        "posts": "posty",
        "comments": "komentarze",
        "debates": "debaty",
        "agents": "aktywni agenci",
        "elapsed": "czas trwania",
        "stop": "Zatrzymaj",
        "stopConfirm": {
          "title": "Zatrzymac symulacje?",
          "description": "Postep zostanie zachowany.",
          "confirm": "Tak, zatrzymaj",
          "cancel": "Kontynuuj"
        }
      },
      "completion": {
        "heading": "Symulacja zakonczona",
        "badge": "Zakonczone",
        "goToReport": "Przejdz do raportu"
      },
      "error": {
        "heading": "Symulacja zakonczyla sie bledem",
        "body": "Wystapil problem podczas symulacji. Sprawdz logi serwera lub sprobuj ponownie.",
        "returnToEnv": "Wroc do srodowiska"
      },
      "empty": {
        "heading": "Laczenie z symulacja...",
        "body": "Trwa inicjalizacja symulacji. To moze potrwac chwile."
      },
      "feed": {
        "twitter": "Twitter",
        "reddit": "Reddit",
        "expand": "wiecej...",
        "replyingTo": "w odpowiedzi na",
        "waitingForPosts": "Oczekiwanie na posty...",
        "newPosts_one": "{{count}} nowy post",
        "newPosts_few": "{{count}} nowe posty",
        "newPosts_many": "{{count}} nowych postow"
      },
      "timeline": {
        "header": "Os czasu",
        "round": "Runda {{round}}",
        "allRounds": "Wszystkie rundy",
        "drawerToggle": "Otworz os czasu",
        "waitingForEvents": "Oczekiwanie na zdarzenia..."
      },
      "actionType": {
        "post": "post",
        "comment": "komentarz",
        "retweet": "retweet",
        "debate": "debata"
      },
      "activeAgents_one": "{{count}} aktywny agent",
      "activeAgents_few": "{{count}} aktywni agenci",
      "activeAgents_many": "{{count}} aktywnych agentow"
    }
    ```

    Create `src/locales/en/simulation.json` with English equivalents:
    ```json
    {
      "progress": {
        "round": "Round {{current}}/{{total}}",
        "posts": "posts",
        "comments": "comments",
        "debates": "debates",
        "agents": "active agents",
        "elapsed": "duration",
        "stop": "Stop",
        "stopConfirm": {
          "title": "Stop simulation?",
          "description": "Progress will be saved.",
          "confirm": "Yes, stop",
          "cancel": "Continue"
        }
      },
      "completion": {
        "heading": "Simulation complete",
        "badge": "Completed",
        "goToReport": "Go to report"
      },
      "error": {
        "heading": "Simulation ended with an error",
        "body": "A problem occurred during simulation. Check server logs or try again.",
        "returnToEnv": "Return to environment"
      },
      "empty": {
        "heading": "Connecting to simulation...",
        "body": "Initializing simulation. This may take a moment."
      },
      "feed": {
        "twitter": "Twitter",
        "reddit": "Reddit",
        "expand": "more...",
        "replyingTo": "replying to",
        "waitingForPosts": "Waiting for posts...",
        "newPosts_one": "{{count}} new post",
        "newPosts_few": "{{count}} new posts",
        "newPosts_many": "{{count}} new posts"
      },
      "timeline": {
        "header": "Timeline",
        "round": "Round {{round}}",
        "allRounds": "All rounds",
        "drawerToggle": "Open timeline",
        "waitingForEvents": "Waiting for events..."
      },
      "actionType": {
        "post": "post",
        "comment": "comment",
        "retweet": "retweet",
        "debate": "debate"
      },
      "activeAgents_one": "{{count}} active agent",
      "activeAgents_few": "{{count}} active agents",
      "activeAgents_many": "{{count}} active agents"
    }
    ```

    Update `src/i18n/config.ts`:
    - Add imports: `import plSimulation from '@/locales/pl/simulation.json'` and `import enSimulation from '@/locales/en/simulation.json'`
    - Add `simulation: plSimulation` to `resources.pl` object
    - Add `simulation: enSimulation` to `resources.en` object
    - Add `'simulation'` to the `ns` array (after 'environment')
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/miroslaw_ryba && node -e "const pl = require('./src/locales/pl/simulation.json'); const en = require('./src/locales/en/simulation.json'); console.log('PL keys:', Object.keys(pl).length); console.log('EN keys:', Object.keys(en).length); console.log('Has plurals:', 'newPosts_one' in pl.feed && 'newPosts_few' in pl.feed && 'newPosts_many' in pl.feed); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - src/locales/pl/simulation.json contains "Runda {{current}}/{{total}}"
    - src/locales/pl/simulation.json contains "newPosts_one"
    - src/locales/pl/simulation.json contains "newPosts_few"
    - src/locales/pl/simulation.json contains "newPosts_many"
    - src/locales/pl/simulation.json contains "activeAgents_one"
    - src/locales/pl/simulation.json contains "Zatrzymaj"
    - src/locales/pl/simulation.json contains "Os czasu"
    - src/locales/en/simulation.json contains "Round {{current}}/{{total}}"
    - src/locales/en/simulation.json contains "Stop simulation?"
    - src/i18n/config.ts contains "import plSimulation from '@/locales/pl/simulation.json'"
    - src/i18n/config.ts contains "simulation: plSimulation"
    - src/i18n/config.ts contains "simulation: enSimulation"
    - src/i18n/config.ts ns array contains 'simulation'
  </acceptance_criteria>
  <done>Polish and English simulation translation files exist with all copy from UI-SPEC including plural forms. i18n config registers the simulation namespace.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/features/simulation/ --reporter=verbose` -- all store and hook tests pass
- `src/features/simulation/hooks/` directory contains 6 hook files
- `src/locales/pl/simulation.json` and `src/locales/en/simulation.json` exist with matching key structure
- `src/i18n/config.ts` registers the simulation namespace
</verification>

<success_criteria>
- Simulation Zustand store created with persist middleware and correct state shape
- Three polling hooks wrap usePollingQuery with staggered intervals (2000/2500/3000ms)
- Elapsed time and scroll anchor utility hooks created
- Polish/English translations complete with plural forms
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-simulation/06-01-SUMMARY.md`
</output>
